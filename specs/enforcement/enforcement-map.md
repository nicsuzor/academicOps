---
id: enforcement-734e5788
title: Enforcement Map (redirect)
type: state
status: superseded
tier: core
supersedes_target: .agents/ENFORCEMENT-MAP.md
depends_on: []
tags: [framework, enforcement, redirect, state]
---

# Enforcement Map — redirect

**This file is no longer canonical.** The operative enforcement map is:

> **`.agents/ENFORCEMENT-MAP.md`**

That file is the single SSoT (state) for the framework's enforcement
catalogue. It carries the L0–L7 cost ladder that `rbg` blocks on (P#65)
and the axiom-keyed rule registry previously kept here was folded into
its **Axiom × mechanism cross-reference** section on 2026-05-20 as part
of `aops-3c665002` / parent epic `aops-2b8dd7a7`.

For design rationale (why enforcement is shaped the way it is, the
pipeline view, the pyramid view, the evidence loop) see
`specs/enforcement/enforcement.md`. For the per-mechanism reference
catalogue keyed to the L0–L11 pipeline view, see
`specs/enforcement/enforcement-mechanisms.md`. Both are spec / design
narrative, not operative tiers — no blocking decision uses them.
