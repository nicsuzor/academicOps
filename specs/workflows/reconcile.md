# Reconcile (Retired)

The reconcile procedure has been converted to a composable workflow template.
This spec is superseded by the `wf-reconcile` template in the universal workflow tier (`plugins/ida/workflows/wf-reconcile.md`). All obligations, status writes, and exit criteria are defined there.
