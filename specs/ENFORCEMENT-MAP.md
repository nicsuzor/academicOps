---
title: Enforcement Map
type: state
category: state
permalink: state-enforcement-map
description: SSoT for the current-state register mapping rules to enforcement mechanisms, triggers, and gate modes across interactive/polecat/subagent surfaces.
---

# Enforcement Map

> **State — operative current-state register.** Which **rule** is reinforced by which **mechanism**, at which pyramid level, _right now_. This file is the single source of truth (SSoT) for the macroscopic matrix mapping rules to enforcement triggers, client differences, and gate modes.
>
> **Where things live now (read this if you came looking for prose):** design reasoning — pyramid theory, escalation discipline, CBA, the coercion-vs-cost axis — is in [`enforcement/enforcement.md`](enforcement/enforcement.md). Per-gate forensic detail (config / verify / debug) is in [`enforcement/GATES.md`](enforcement/GATES.md). Per-agent detail is in `specs/agents/*.md`. Hook wire-channel detail (which field reaches user vs agent) is in [`CLIENT-TRANSLATION.md`](CLIENT-TRANSLATION.md). `rbg` blocks on this file's currency (P#65).

---

## 1. Unified SSoT Matrix: Rules, Mechanisms, and Triggers

This matrix is the centralized authoritative register tracking every core rule, how it is enforced (the mechanism), when it fires (trigger), and its mode across interactive vs. autonomous surfaces. **Design intent is client-agnostic** — per-client wire differences are not behavioural intent and never appear here as rows or columns; they live as capability caveats in [`CLIENT-TRANSLATION.md`](CLIENT-TRANSLATION.md). (The Mode columns below are _surface_ distinctions — interactive vs. background vs. subagent — not clients.)

| Rule (Axiom/Directive)                           | Mechanism / Gate                                                                                          | Trigger                                                                    | Level         | Mode: Interactive (`ida`) | Mode: Polecat (Background) | Mode: Subagents | Notes                                                                                                                                                                                                                                                                                                                                                                               |
| :----------------------------------------------- | :-------------------------------------------------------------------------------------------------------- | :------------------------------------------------------------------------- | :------------ | :------------------------ | :------------------------- | :-------------- | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `data-boundaries`, `evidence-immutable`          | `sentinel` gate                                                                                           | **PreToolUse:** destructive verb on protected path                         | L4            | `block`                   | `block`                    | _never fires_   | Hard deny; user sees the block. PreToolUse is skipped for subagent-attributed events — see note below table.                                                                                                                                                                                                                                                                        |
| `all axioms`                                     | `enforcer` gate                                                                                           | **PreToolUse:** every N (default 50) write ops                             | L4→L6         | `warn` (default)          | `warn` (default)           | _never fires_   | Subagent dispatch (the parent's `Agent(...)` call) resets the parent's threshold; the subagent's own tool calls never reach this gate — see note below table.                                                                                                                                                                                                                       |
| `all axioms`                                     | `rbg-review` gate                                                                                         | **Stop:** while task is bound                                              | L2→L6         | OPEN (inert)              | `block`                    | `block`         | Held `CLOSED` until `rbg` runs. Suppressed while the session is **paused** — waiting on a background `subagent`/`workflow`/`monitor` or a `session_cron` (Claude Code 2.1.145+; see `_dispatch_gates`).                                                                                                                                                                             |
| `exercise-authority`, show-don't-tell            | `qa` gate                                                                                                 | **Stop:** after task claimed `in_progress`                                 | L2            | `warn`                    | `warn`                     | `warn`          | `warn` now DENYs once (forces one continuation) then latches open, like `handover`/`ida` — no longer a soft advisory. Reopens when `qa`/`verify`/`marsha` runs. Suppressed while the session is **paused** (see `rbg-review`).                                                                                                                                                      |
| `halt-on-failure`                                | `handover` gate                                                                                           | **Stop:** if work was done (task/write tool)                               | L2            | `warn` (hard-block-once)  | `block`                    | `warn`          | `warn` now DENYs once (forces one continuation) then latches open — no longer a soft `additionalContext` merge; the `mem-438429c5` soft/rate-limited nudge is superseded. Reopens when `/end-session`, `/dump`, or `/continue` (pause — work in progress, task not concluded) runs. Suppressed while the session is **paused** (see `rbg-review`).                                  |
| `honest-epistemics`                              | `ida` gate                                                                                                | **Stop:** first stop per turn                                              | L2            | `warn`                    | `warn`                     | `warn`          | Fire-once per turn, hard-block-once (warn now emits DENY; on Claude it stays user-quiet via asyncRewake keyed on warn-mode). Also opens on `/continue` (the pause skill delivers the honest resume summary, so the honesty block must not re-fire). Suppressed while the session is **paused** (see `rbg-review`).                                                                  |
| `honest-epistemics`, `single-source-of-truth`    | `pkb.nudge`                                                                                               | **UserPromptSubmit**                                                       | L2            | advisory                  | n/a                        | advisory        | Injected into agent context.                                                                                                                                                                                                                                                                                                                                                        |
| `do-one-thing`                                   | `hydration.warn`                                                                                          | **UserPromptSubmit:** main session only                                    | L2            | advisory                  | n/a                        | advisory        | Routing hints.                                                                                                                                                                                                                                                                                                                                                                      |
| `halt-on-failure`, `data-boundaries`             | Safety floor (`CORE.md`)                                                                                  | **SessionStart:** `@`-import                                               | L1            | instruction               | instruction                | instruction     | One injected copy for every surface.                                                                                                                                                                                                                                                                                                                                                |
| `single-source-of-truth` (related-work cohesion) | `enforcer`/`rbg` review + `cohesive-pr-epic.md`                                                           | **PR-time:** a coupled task set must share ONE draft PR                    | L7            | review                    | review                     | review          | Always-on rule.                                                                                                                                                                                                                                                                                                                                                                     |
| `honest-epistemics`                              | PKB tool boundary (`release_task`/`complete_task`)                                                        | **Tool call:** task release/completion                                     | L2            | enforced                  | enforced                   | enforced        | `summary` required on release; `pr_url` (if given) live-verified via `gh` before acceptance — the anti-fabrication integrity gate (`polecat/pkb_bridge.py`).                                                                                                                                                                                                                        |
| `judgment-non-delegable`                         | [premise gate](enforcement/premise-gate.md) — `rbg` + `pauli` clearance via `/strategic-review --premise` | Spend surfaces (`/pull`, `/dispatch`, `/supervisor` dispatch), pre-compute | dispatcher L4 | `block`                   | `block`                    | `block`         | Two-judge agent clearance, not a hook: dispatcher ensures a legible premise, then `rbg` (axiom/rig) + `pauli` (worth/shape) each return CLEAR/BOUNCE; any BOUNCE hard-refuses. Reuses the `/strategic-review` judges (DRY); the same judgment re-fires on completed work at the review surface. Enforcement attaches at dispatch, not at the hookless manual `→ queued` transition. |

> **Subagent surface, precisely:** any event tagged `is_subagent` (a dispatched Task/Agent child, or an agy worker — `AOPS_AGY_CLIENT=1` forces this posture for the worker's whole run since it can't action an interactive prompt) skips gate evaluation for **`PreToolUse`/`PostToolUse` only**; `Stop`/`SessionEnd`/`SubagentStop`/`UserPromptSubmit` still fire (`router.py:_dispatch_gates`). Since `sentinel`/`enforcer` are the only PreToolUse-triggered gates, they simply never run on a subagent's/worker's own tool calls — the "Subagents" column above is inert for those two rows, not a live `block`/`warn`. Forensic detail: [`enforcement/GATES.md`](enforcement/GATES.md) § Subagent & worker session scope.

### 1.1 Per-message routing (agent-first)

Every hook message is enforcement **directed at the agent** — the **agent channel is non-negotiable and always present** (the `Agent template` column, which is the `CONTEXT_INJECTION` template the agent reads). What is _optional_ is whether anything also reaches the **user terminal**:

- **`silent`** — nothing rendered to the user; the agent handles it.
- **`same`** — the user sees the same text the agent sees (no separate template).
- **`[[file]]`** — a user-specific `USER_MESSAGE` template (the linked `*.policy_message` file).

This table owns the **disposition** (who-sees-what); wire-mechanics and per-client capability detail live in [`CLIENT-TRANSLATION.md`](CLIENT-TRANSLATION.md).

| Gate · fire (event)                           | Agent template (always)                                                                      | User message                                                                        | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| :-------------------------------------------- | :------------------------------------------------------------------------------------------- | :---------------------------------------------------------------------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `sentinel` · deny (PreToolUse)                | [sentinel-policy-context](../aops-core/hooks/templates/sentinel-policy-context.md)           | `same`                                                                              | Hard-deny on a protected path — the **one** case the user genuinely needs the banner.                                                                                                                                                                                                                                                                                                                                                              |
| `enforcer` · block (PreToolUse)               | [rbg-policy-context](../aops-core/hooks/templates/rbg-policy-context.md)                     | `silent`                                                                            | Periodic compliance plumbing; outcome + hook in transcript only.                                                                                                                                                                                                                                                                                                                                                                                   |
| `enforcer` · countdown (PreToolUse run-up)    | —                                                                                            | `silent`                                                                            | "N calls until the check" is pure user noise.                                                                                                                                                                                                                                                                                                                                                                                                      |
| `enforcer` · dispatch (PreToolUse)            | [rbg-instruction](../aops-core/hooks/templates/rbg-instruction.md)                           | `silent`                                                                            | Instruction to invoke the enforcer subagent.                                                                                                                                                                                                                                                                                                                                                                                                       |
| `qa` · block (Stop)                           | [qa-policy-context](../aops-core/hooks/templates/qa-policy-context.md)                       | `silent`                                                                            | "Verify before you stop" is agent-directed.                                                                                                                                                                                                                                                                                                                                                                                                        |
| `handover` · block (Stop)                     | [stop-gate-handover-block](../aops-core/hooks/templates/stop-gate-handover-block.md)         | `silent`                                                                            | Agent-directed handover instruction.                                                                                                                                                                                                                                                                                                                                                                                                               |
| `ida` · reminder (Stop)                       | [ida-reminder](../aops-core/hooks/templates/ida-reminder.md)                                 | one-line summary                                                                    | Fires a hard-block-once DENY; on Claude it takes the asyncRewake quiet-split (full body → agent, one-line → user), keyed on ida **warn-mode** (not verdict==WARN). Biggest user-noise cut of this set.                                                                                                                                                                                                                                             |
| `ida` · AskUserQuestion nudge (PreToolUse)    | [ida-askuserquestion-reminder](../aops-core/hooks/templates/ida-askuserquestion-reminder.md) | `silent`                                                                            | Already agent-only.                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `rbg_review` · block (Stop)                   | [rbg-policy-context](../aops-core/hooks/templates/rbg-policy-context.md)                     | `silent`                                                                            | Polecat/crew-only, agent-directed.                                                                                                                                                                                                                                                                                                                                                                                                                 |
| block-gate · degraded escape-hatch (Stop)     | —                                                                                            | `keep` ([rbg-review-degraded](../aops-core/hooks/templates/rbg-review-degraded.md)) | Mechanism-class row: every block-mode Stop gate (`rbg_review`@5, plus `qa`/`handover`@engine-default 3) downgrades `DENY`→`WARN`-and-allow after N consecutive unsatisfied Stops in a turn (per-turn counter, resets on UPS). Loud **by design** — the user **should** see it when a gate gives up. Replaces the deleted router `stop_hook_active` bypass as the client-agnostic loop guard (with the 5-blocks-in-2-min override as residual net). |
| `pkb` · nudge (UserPromptSubmit)              | [pkb-nudge](../aops-core/hooks/templates/pkb-nudge.md)                                       | `silent`                                                                            | Already agent-only.                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `hydration` · routing hint (UserPromptSubmit) | [hydration-gate-warn](../aops-core/hooks/templates/hydration-gate-warn.md)                   | `silent`                                                                            | Routing hints are agent-directed context injection, not a user message.                                                                                                                                                                                                                                                                                                                                                                            |

**Status / transition pings** (`allow` verdict — informational, not enforcement). Default `silent`; the agent already knows the gate cleared:

| Ping                        | User message | Notes                                             |
| :-------------------------- | :----------- | :------------------------------------------------ |
| `enforcer.verified` (reset) | `silent`     | Counter reset — agent-internal.                   |
| `qa.complete`               | `silent`     | Verifier ran.                                     |
| `handover.bound`            | `silent`     | Task bound.                                       |
| `handover.complete`         | `keep`       | Session-end confirmation — user-useful, low-rate. |
| `rbg_review.complete`       | `silent`     | rbg ran, gate cleared.                            |

> **Out of scope:** `SUBAGENT_INSTRUCTION` templates (`enforcer.context`, `qa.context`, `enforcer.audit`, `rbg_review.context`) reach **neither** the user nor the main agent — they are written to the temp file a _dispatched_ subagent reads. No user/agent disposition applies.

---

## 2. Capability & Identity Restrictions (Tool Access)

Tool and identity restrictions serve as foundational enforcement mechanisms, restricting what agents can do based on their role.

| Restriction Area                          | Enforcement Rule                                                                                                                                                                                                                                                                   | Applied To                                                                                 |
| :---------------------------------------- | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :----------------------------------------------------------------------------------------- |
| **Specialized Execution (Playwright)**    | Playwright and heavy e2e testing tools are strictly gated to specialized validation agents (`marsha`), enforcing separation of authoring vs QA.                                                                                                                                    | Excludes: `ida`, `pauli`, `rbg`                                                            |
| **PKB Knowledge Write**                   | While baseline agents (`ida`, `pauli`, `james`, `marsha`, `rbg`) share `mcp__plugin_aops-core_pkb__*` access, writes are constrained by `data-boundaries`.                                                                                                                         | Enforced via `rbg` review & `data-boundaries`                                              |
| **Baseline Tooling**                      | Universal access to `Bash`, `Edit`, `Grep`, `Read`, `Skill` is granted across interactive (`ida`) and background workers to ensure `full-observability`.                                                                                                                           | All Authoring Agents                                                                       |
| **Command-Level Skill-Only Funnel**       | A slash-command's frontmatter `allowed-tools` is narrowed to `Skill` only, forcing every execution path through the delegated skill so the command file stays a thin wrapper with no independent tool surface (a chokepoint/funnel, L4).                                           | `dispatch.md`, `maintain.md`, `pull.md`                                                    |
| **Comment-Triggered Agent Identity Gate** | An `@claude` mention that would grant write/Bash access is restricted to `COLLABORATOR`/`MEMBER`/`OWNER` comment/review authors (`author_association`), matching `trigger-enforcer.yml`/`trigger-qa.yml`'s allowlist — an arbitrary commenter cannot invoke a write-capable agent. | `claude.yml` (`claude` job only — `claude-issue-triage` is read-only regardless of author) |

> **Note on Voluntary Skills (L3)**: Voluntary skills (e.g., `/verify`, `/design-rubric`, `/q`) are **capabilities, not enforcement mechanisms**. The restrictions on their use, and the hooks (like `qa` gate) that remind agents to use them, constitute the actual enforcement.

---

## 3. PR / merge pipeline (current state)

The mechanisms that fire on a PR / at merge. Terse current-state rows; the canonical contract is [`workflows/pr-pipeline.md`](workflows/pr-pipeline.md), per-agent detail is in `specs/agents/*.md`.

| Mechanism                        | Level | Required at merge?          | What it does                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| :------------------------------- | :---- | :-------------------------- | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `enforcer-status` (rbg)          | L6    | **yes**                     | LLM axiom review of HEAD diff; auto on PR events, manual `/enforce`. Blocks on CHANGES_REQUESTED/failure — but only for violations left **unresolved**; a violation the agent found and fixed itself in the same pass (committed) never blocks, even though the fix commit means the posted verdict lands on the next SHA, not this one (pr-pipeline §3.4 pt 6, §4.2; `scripts/ci/enforcer-terminal-status.sh`). If `gh pr review` fails because the PR's own author identity collides with the agent's (`claude[bot]`), the verdict is recovered from a structured fallback PR comment instead of deadlocking at an unrecoverable red (pr-pipeline §4.2 "Self-review identity-collision fallback"; `enforcer.agent.md` §5a).                                   |
| `qa-status` (marsha)             | L6    | **yes**                     | Runtime verification ("broken until proven"); auto on PR events, manual `/qa`/`/verify`. Same self-review identity-collision fallback as `enforcer-status` above (pr-pipeline §4.2; `qa.agent.md`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `admit-status` (human)           | L7    | **yes**                     | The human "good idea — make it mergeable" decision: `admit-on-review.yml` for normal PRs; `conflict-admission-sweep.yml` (deferred execution of the standing approval) for conflicting PRs that cannot receive `pull_request_review` events. Both call the same `scripts/ci/admit-pr.sh` (POST admit-status + arm `--auto --squash`) and the same `scripts/ci/reviewer-authz.sh` (write-class-or-allowlisted check) so the grant and the policy can't drift between the two origins (pr-pipeline §5.1). **Sticky**: once admitted, carries forward across every push until a terminal state (merge / §3.6 exhaustion / §3.10 changes-requested); a push never re-judges admission (pr-pipeline §5).                                                             |
| Lint / Pytest                    | L4    | **yes**                     | Mechanical CI.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `alignment-status` (pauli)       | L6    | no — **advisory by design** | PKB design-intent review (NOT axioms). Queue surface live; host dispatch spec-only. Informs the human admit gate.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `responder-status` (§3.8)        | L4    | no — informational          | Pre-admission mechanical fix: typos, failing CI (including PR-attributable `Pytest` red — `check-mechred needs: [..., pytest]`, `PYTEST_RESULT` injected; base-broken guard prevents herd on shared failures). NOT merge conflicts — a conflicting PR never reaches it (§3.11). `MAX_RESPONDER_RUNS=3`; judgment calls surface to human unmodified.                                                                                                                                                                                                                                                                                                                                                                                                             |
| `mechanic-status`                | L4    | no — informational          | Post-admission dev + conflict-resolve (the ONLY conflict resolver; a conflicting PR is reached only via the conflict-admission sweep — `conflict-admission-sweep.yml`, §3.11); `MAX_MECHANIC_RUNS=5`; enforcer+qa re-verify each SHA.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `claude.yml` (`@claude` mention) | L4    | no — on-demand assistant    | Unattended dev assistant triggered by an `@claude` mention (PR review comment, PR review, or PR-scoped issue comment) or manual `workflow_dispatch`; same capability class as `mechanic-status`: `contents: write`, PAT-backed full-history checkout, `--allowed-tools "Bash,Read,Edit,Write"` (added PR #2078 — closed a gap where it could diagnose a stuck PR, e.g. a merge conflict, but not fetch/rebase/push a fix). Gated to `COLLABORATOR`/`MEMBER`/`OWNER` triggers (§2). A bare-issue `@claude` mention runs the separate `claude-issue-triage` job instead — read-only, no PAT, no `--allowed-tools` — because a plain issue has no PR head branch to scope a checkout to (it would land on the default branch). Posts no status; never gates merge. |
| `review-attestation`             | L7    | in-ruleset (apply pending)  | Fail-closed proof a _named_ reviewer ran on _this_ SHA. On failure, writes a cause+remedy breakdown to the Actions run summary (points at Force Review, pr-pipeline §3.7).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `comment-triage-status`          | L6    | in-ruleset (apply pending)  | Fail-closed gate over unresolved third-party review comments/reviews (Copilot, Gemini, ...) — restores the v1 merge-prep invariant ("read ALL reviews", F5) that PR #2094 exposed as silently dropped when merge-prep was retired at Phase 5. Runs on every push (`pr-pipeline.yml`) and at admission (`admit-on-review.yml`'s `admit-comment-triage`); also feeds `decide-mechanic`'s dispatch, which sends the mechanic in `review-response` mode when this is the only red signal (`scripts/ci/check-unresolved-comments.sh`, unit-tested).                                                                                                                                                                                                                  |
| branch protection                | L7    | **yes**                     | AND-gates the required checks (active ruleset `13762049`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| CODEOWNERS review request        | L2    | no — surfacing              | `.github/CODEOWNERS` (`* @nicsuzor`) auto-requests the maintainer's review on PR open, routing PRs to the L7 human admit gate. Request-only; NOT wired as a "require code-owner review" branch-protection rule (pr-pipeline §3.2).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Force Review (escape hatch)      | L4    | no — manual re-trigger      | `force-review.yml` (`workflow_dispatch`) re-runs the enforcer/qa merge-gate reviewers on a wedged PR's HEAD via the same reusable workflows. Bypasses admission ON PURPOSE (does not merge — merge still needs all required checks green + `admit-status`); sibling of `/enforce`·`/qa` manual triggers (pr-pipeline §3.12).                                                                                                                                                                                                                                                                                                                                                                                                                                    |

> Local/off-pipeline routes to the same agents: `/strategic-review` → james (deploys rbg+pauli+marsha, reconciles APPROVE/REVISE/ESCALATE, advisory); `/verify` → marsha; `/enforce` PR comment → rbg.

---

## Known gaps

- **Axioms are enforced reactively, not delivered always-on.** No hook injects the axiom set at SessionStart; no working agent `@`-imports `AXIOMS.md` — only `rbg` does (as its review reference, not "told to obey"). `.agents/CORE.md` restates `halt-on-failure`/`data-boundaries` operationally and carries the **universal safety floor** — Safety Invariants (no credential read/store/broker) + PKB-HALT — as the one injected copy for every surface. **Injection-path caveat:** `session_env_setup.py` reads `aops-core/CORE.md`, which does NOT exist in-repo, so in dev/local sessions CORE.md reaches the model via the `@`-import in `CLAUDE.md`/`GEMINI.md`, not that hook read. Coverage is verified per-surface: polecat workers' PKB-HALT floor is asserted by `tests/polecat/test_prompt_template.py::test_prompt_carries_pkb_halt_floor` (checks `PKB_HALT_FLOOR` reaches `build_polecat_prompt` output in both task and issue modes); interactive/in-repo-agent PKB-HALT coverage is the `.agents/CORE.md` prose itself (§ PKB-HALT), the one injected copy for that surface — no separate per-agent duplicate. Enforcement is the periodic enforcer gate plus the per-rule rows in [Unified Matrix](#1-unified-ssot-matrix-rules-mechanisms-and-triggers).
- **Auto-mode classifier seeded and populated** (L5). The polecat `autoMode` ruleset (`polecat/defaults/claude-settings.json`) carries `environment`/`allow`/`soft_deny` entries — the individual rules are maintained directly in that file, not enumerated per-rule here or in the canonical spec, per the mechanism-class grain this map tracks. New rules still follow the evidence loop before landing. Canonical: [`auto-mode-classifier.md`](enforcement/auto-mode-classifier.md).
- **Hydration / commit gates**: env vars wired (`HYDRATION_GATE_MODE`, `COMMIT_GATE_MODE`) but no `GateConfig` body — stubs.
- **Evidence loop steps 4–5** (`/aops` pattern detection → recommendation) aspirational; capture (step 2 `/learn`) and implementation (step 6) exist. Step 7 auto-map-update is manual.
- **`orchestrator-boundary-warn.md`** — template file not in `TEMPLATE_SPECS`; likely dead/unwired (cleanup follow-up).
- **PTY harness coverage** — measures Claude user-visibility + Stop agent-context; agy wire-acceptance, `--resume` persistence, and a model-echo agent-context lane for UPS/PreToolUse are not yet ported (`scripts/pty_hook_probe.py` is `client`-parameterized for it).
