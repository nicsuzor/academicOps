# Enforcement Map

> **State — operative current-state register.** Which **rule** is reinforced by which **mechanism**, at which pyramid level, _right now_. Three current-state views:
>
> 1. **[Hook injection trace](#1-hook-injection-trace)** — the substantive point each runtime hook pushes into agent context, and when (the high-volume base/middle of the pyramid). This is the layer most people want: "what is the framework actually saying to the agent, and at what moment?"
> 2. **[Rule → mechanism matrix](#2-rule--mechanism-matrix)** — for each axiom / project-rule, the mechanisms that reinforce it across the pyramid. Axioms are _legislative_; they don't enforce themselves — this is where you see how each one is _executed_.
> 3. **[Pyramid placement register](#3-pyramid-placement-register-l0l7)** — every mechanism's L0–L7 position (the register-of-record for add/escalate/remove decisions).
>
> **Where things live now (read this if you came looking for prose):** design reasoning — pyramid theory, escalation discipline, CBA, the coercion-vs-cost axis — is in [`enforcement/enforcement.md`](enforcement/enforcement.md). Per-mechanism schema detail (trigger / location / scope / status) is in [`enforcement/enforcement-mechanisms.md`](enforcement/enforcement-mechanisms.md). Per-gate forensic detail (config / verify / debug) is in [`GATES.md`](GATES.md). Hook wire-channel detail (which field reaches user vs agent) is in [`hooks/CLIENT-TRANSLATION.md`](hooks/CLIENT-TRANSLATION.md). This file links rules ↔ mechanisms and nothing else. `rbg` blocks on its currency (P#65).

---

## 1. Hook injection trace

The runtime hook layer is the framework's **highest-volume enforcement surface** — it fires on every prompt, every Nth tool call, and every exit. Each row is one **substantive point** the framework injects, the **hook event** that delivers it (the _when_), the **mechanism + pyramid level**, the **audience** (**U**ser terminal vs **A**gent context — measured live, see [`hooks/CLIENT-TRANSLATION.md`](hooks/CLIENT-TRANSLATION.md)), and the **rule(s)** it reinforces.

Pyramid level is the **L0–L7 register** in [§3](#3-pyramid-placement-register-l0l7) (NOT the L0–L11 _pipeline_ numbering — see [`enforcement.md`](enforcement/enforcement.md) §3 for why the two differ). The Stop gates `ida`/`qa`/`handover` **inject** at **L2** (lifecycle reminder) and **escalate to a hard block (tip)** only in block-mode — both shown as "L2→tip".

| Substantive point injected                                                                                                                              | Hook · when                                               | Mechanism · pyramid level                             | Audience | Reinforces                                                                         |
| :------------------------------------------------------------------------------------------------------------------------------------------------------ | :-------------------------------------------------------- | :---------------------------------------------------- | :------- | :--------------------------------------------------------------------------------- |
| The agent's role, operative rules (`halt-on-failure`, `data-boundaries`), status strip                                                                  | **SessionStart**                                          | persona + `.agents/CORE.md`/`AGENTS.md` load · **L1** | A        | the agent's whole rule-frame                                                       |
| **Universal safety floor** — Safety Invariants (no credential read/store/broker) + PKB-HALT — the ONE injected copy for every surface (D1, safety SSoT) | **SessionStart**                                          | `.agents/CORE.md` (`@`-import) · **L1**               | A        | `data-boundaries`, `halt-on-failure`                                               |
| "Search the PKB before answering from memory or assuming"                                                                                               | **UserPromptSubmit** · every prompt (main + subagent)     | `pkb.nudge` · **L2**                                  | A        | `single-source-of-truth`, `honest-epistemics`                                      |
| "Here are the skills relevant to this intent" (routing table)                                                                                           | **UserPromptSubmit** · every main-session prompt          | `hydration.warn` · **L2**                             | A        | `do-one-thing`, correct-procedure                                                  |
| "Destructive op on a protected user-env path — blocked"                                                                                                 | **PreToolUse** · destructive verb/write on protected path | `sentinel` gate · **L4** (mechanical deny)            | U+A      | `data-boundaries`, `evidence-immutable`                                            |
| "N tool calls until the periodic compliance check"                                                                                                      | **PreToolUse** · within 7 of threshold                    | `enforcer.countdown` · **L2**                         | U        | all axioms (advance warning)                                                       |
| "Periodic compliance check overdue — invoke the enforcer/rbg"                                                                                           | **PreToolUse** · every N non-read tool calls              | `enforcer` gate (L4 trigger) → **L6** rbg audit       | U+A      | all axioms (periodic audit)                                                        |
| "Verify the blocker is real before asserting it (`only you can` / `I can't run X` / `needs auth`)"                                                      | **PreToolUse** · `AskUserQuestion` while ida active       | `ida` AskUserQuestion trigger · **L2**                | A        | `honest-epistemics`, `exercise-authority`                                          |
| "Back assertions with proof; be honest before exit; no criterion substitution"                                                                          | **Stop** · every armed turn                               | `ida` gate · **L2→tip**                               | U+A      | `honest-epistemics`                                                                |
| "Verify before you stop (run the QA verifier)"                                                                                                          | **Stop** · while a task is bound                          | `qa` gate · **L2→tip** → **L6** marsha                | U+A      | `exercise-authority`, show-don't-tell                                              |
| "Produce a structured handover before exit (commit/push/reflect)"                                                                                       | **Stop** · work-turns (`session_did_work`)                | `handover` gate · **L2→tip**                          | U+A      | `halt-on-failure` (no work loss)                                                   |
| "Run the final axiom audit before this task-bound session exits"                                                                                        | **Stop** · polecat/crew exit                              | `rbg-review` gate (tip block) → **L6** rbg audit      | U+A      | all axioms (exit backstop)                                                         |
| _(per-action judgment gate — wired but **unseeded**)_                                                                                                   | **PreToolUse** · every tool call (auto-mode)              | auto-mode classifier · **L5**                         | A        | none seeded (see [`auto-mode-classifier.md`](enforcement/auto-mode-classifier.md)) |

> **AXIOMS.md is NOT in this trace** — no hook injects the axiom set at SessionStart; only `rbg` `@`-imports it as its review reference. Axioms reach working agents only indirectly (via `CORE.md` restating `halt-on-failure`/`data-boundaries`, and via the enforcer/rbg audits above). This is a known gap, not an omission from the table — see [Known gaps](#known-gaps).

### 1a. Firing & cadence detail

The precise lifecycle for each Stop/PreToolUse mechanism above — gate status that triggers it, mode→verdict, exact cadence/rate-limit. Source: `aops-core/lib/gates/definitions.py` (gates), `aops-core/lib/template_registry.py` (template keys→files), `aops-core/lib/gates/custom_conditions.py` (cadence). User-visibility is **empirically measured** on a live TTY by `scripts/pty_hook_probe.py` → `tests/hooks/fixtures/pty_capabilities.json`.

**Key cadence facts (the contrast that matters):**

- **enforcer** — fires every **N** qualifying (write/non-read) tool calls (N = `ENFORCER_TOOL_CALL_THRESHOLD`; **code default 50** per `gate_config.py:199`, deployment-tunable); resets when the enforcer/rbg subagent runs.
- **handover (block / polecat)** — **block-once per turn** (fire-once on first Stop; a retried Stop in the same turn passes), re-armed each UPS; **no multi-turn rate-limit**.
- **handover (warn / interactive)** — soft nudge **at most once per 5 turns** (`_HANDOVER_WARN_TURN_CADENCE`); first fire never suppressed.
- **rbg-review** — **no turn cadence**; re-fires _every_ Stop until rbg actually runs (deliberately omits the fire-once the other Stop gates have), with a loud escape-hatch downgrade to WARN-allow after **5 consecutive Stop-blocks in one turn** (`RBG_REVIEW_DEGRADE_THRESHOLD`).
- **qa** — **once per turn** (fire-once per UPS→Stop cycle), only when a task is bound.
- **ida** — fires once per turn on the armed Stop (fire-once), re-armed every **non-slash-command** UPS — the always-on honesty floor, **no multi-turn cap**.

**Measured audience facts (live TTY, Claude Code 2.1.193):** a fired Stop gate emits **two** payloads — a short `systemMessage` banner (`Stop says: …`, U-only) **and** the advisory (`context_injection`). On Claude the advisory is **also user-visible** (`Stop hook error:` in block mode, `Stop hook feedback:` in warn mode) — so **warn mode is NOT quieter than block for the user**; the mode changes only the transcript label and whether the agent is hard-blocked. `suppressOutput:true` does **not** hide the structured notice. (Corrected 2026-06-26, task aops-4de68b25 — the earlier "warn = quiet" premise was false.)

<details><summary>Full template firing table (all 27 templates × when · who · cadence)</summary>

**Audience legend.** **U** = rendered to the user's terminal. **C** = injected into model/agent context.

| Template (key · file)                                            | Gate · event                             | Fires exactly when                                                                | Verdict (mode)                                                                  | Audience (Claude)                  | Cadence / rate-limit                                                                 |
| :--------------------------------------------------------------- | :--------------------------------------- | :-------------------------------------------------------------------------------- | :------------------------------------------------------------------------------ | :--------------------------------- | :----------------------------------------------------------------------------------- |
| `pkb.nudge` · pkb-nudge.md                                       | router UPS inject · UserPromptSubmit     | every real user prompt (main **and** subagent); skipped for `<task-notification>` | advisory (no verdict)                                                           | **C**                              | every prompt                                                                         |
| `hydration.warn` · hydration-gate-warn.md                        | router UPS inject · UserPromptSubmit     | every real user prompt, **main session only**                                     | advisory (no verdict)                                                           | **C**                              | every main-session prompt                                                            |
| `sentinel.policy_message` · sentinel-policy-message.md           | sentinel · PreToolUse                    | destructive verb + protected path (shell), or any write to a protected path       | DENY (block, code-default)                                                      | **U+C** (permissionDecisionReason) | every matching call (stateless)                                                      |
| `sentinel.policy_context` · sentinel-policy-context.md           | sentinel · PreToolUse                    | (same)                                                                            | DENY                                                                            | **C** (additionalContext)          | every matching call                                                                  |
| `enforcer.countdown` · enforcer-countdown.md                     | enforcer · PreToolUse                    | count reaches `threshold − 7` … `threshold`                                       | warn banner                                                                     | **U**                              | each call in the 7-call run-up                                                       |
| `enforcer.policy_message` · enforcer-policy-message.md           | enforcer · PreToolUse                    | ops-since-reset ≥ threshold, not mid-edit, excl. read/infra/always tools          | DENY (block) / WARN (**code-default warn**)                                     | **U+C** (permissionDecisionReason) | every N tool calls (N=threshold); resets when enforcer/rbg runs                      |
| `enforcer.policy_context` · enforcer-policy-context.md           | enforcer · PreToolUse                    | (same)                                                                            | DENY (block) / WARN (default)                                                   | **C**                              | every N tool calls                                                                   |
| `enforcer.verified` · enforcer-verified.md                       | enforcer · PreToolUse/SubagentStart/Stop | enforcer/rbg subagent runs → counter reset                                        | status (allow)                                                                  | **U**                              | once per reset                                                                       |
| `enforcer.context`/`.instruction`/`.audit`                       | enforcer (subagent dispatch)             | enforcer subagent dispatched                                                      | subagent instruction                                                            | dispatched subagent only           | per dispatch                                                                         |
| `rbg_review.policy_message` · rbg-review-policy-message.md       | rbg-review · Stop                        | gate CLOSED, **polecat/crew only**, at exit                                       | DENY (block, code-default)                                                      | **U** (banner)                     | **every Stop until rbg runs** — no turn cadence                                      |
| `rbg_review.policy_context` · rbg-review-policy-context.md       | rbg-review · Stop                        | (same)                                                                            | DENY / WARN                                                                     | **U+C**                            | every Stop until rbg runs; escape-hatch → WARN-allow after 5 blocks/turn             |
| `rbg_review.complete` · rbg-review-complete.md                   | rbg-review · Subagent/Post               | rbg ran → gate clears (sticky until UPS)                                          | status (allow)                                                                  | **U**                              | once per clear                                                                       |
| `rbg_review.degraded` · rbg-review-degraded.md                   | rbg-review · Stop (escape hatch)         | 5 consecutive Stop-blocks in one turn                                             | WARN-and-allow                                                                  | **U**                              | once per degrade                                                                     |
| `rbg_review.context` · rbg-review-context.md                     | rbg-review (subagent dispatch)           | turn record for rbg subagent                                                      | subagent instruction                                                            | dispatched rbg only                | per dispatch                                                                         |
| `qa.policy_message` · qa-policy-message.md                       | qa · Stop                                | gate CLOSED (task claimed `in_progress`); no-task sessions skip QA                | DENY (block) / — (warn)                                                         | **U** (banner)                     | **once per turn** (fire-once; re-arms each UPS while bound)                          |
| `qa.policy_context` · qa-policy-context.md                       | qa · Stop                                | (same)                                                                            | DENY (block) / WARN (default)                                                   | **U+C**                            | once per turn                                                                        |
| `qa.complete` · qa-complete.md                                   | qa · Subagent/Post                       | marsha/qa/verify subagent runs (sticky until UPS)                                 | status (allow)                                                                  | **U**                              | once per verifier run                                                                |
| `qa.context` · qa-context.md                                     | qa (subagent dispatch)                   | context for qa subagent                                                           | subagent instruction                                                            | dispatched qa only                 | per dispatch                                                                         |
| `handover.policy_message` · handover-policy-message.md           | handover · Stop                          | gate CLOSED **and `session_did_work`**; read-only exempt                          | DENY (block) / WARN (warn)                                                      | **U** (banner)                     | block: once/turn · warn: per cadence                                                 |
| `stop.handover_block` · stop-gate-handover-block.md              | handover · Stop                          | (same)                                                                            | **block (polecat): DENY** · **warn (interactive): WARN soft-once-then-release** | **U+C**                            | **block: once per turn** (fire-once), re-armed each UPS · **warn: once per 5 turns** |
| `handover.bound` · handover-bound.md                             | handover · PostToolUse                   | task bound `in_progress` (polecat/crew)                                           | status (allow)                                                                  | **U**                              | once per bind                                                                        |
| `handover.complete` · handover-complete.md                       | handover · Post(skill)/UPS               | `/end_session`,`/dump`,`/handover` completes                                      | status (allow)                                                                  | **U**                              | once per handover                                                                    |
| `ida.reminder` · ida-reminder.md                                 | ida · Stop                               | gate CLOSED — armed from start, re-armed every **non-slash** UPS (all sessions)   | **block: DENY** · **warn (default): WARN**                                      | **U+C**                            | every armed turn — **no multi-turn cadence**                                         |
| `ida.policy_message` · ida-policy-message.md                     | ida · Stop                               | (same)                                                                            | DENY (block) / — (warn)                                                         | **U** (banner)                     | every armed turn                                                                     |
| `ida.askuserquestion_reminder` · ida-askuserquestion-reminder.md | ida · PreToolUse                         | `AskUserQuestion` while ida active (all sessions)                                 | advisory (allow — never denies)                                                 | **C**                              | every AskUserQuestion                                                                |

**Completeness:** every `TEMPLATE_SPECS` key is represented. One template file is NOT in the registry — `orchestrator-boundary-warn.md` appears only in an `engine.py` comment (likely dead/unwired; flagged for cleanup).

</details>

---

## 2. Rule → mechanism matrix

For each rule, the mechanisms that reinforce it, by pyramid band (**L1–L2 inject** · **L4–L5 gate/block** · **L6–L7 agent/PR review**). A rule is reinforced at _multiple_ levels by design (defence-in-depth) — that is the point of the matrix. Cells name the mechanism; detail is one click away in [§3](#3-pyramid-placement-register-l0l7) / [`enforcement-mechanisms.md`](enforcement/enforcement-mechanisms.md).

| Rule (axiom slug)        | Inject (L1–L2)                  | Gate / block (L4–L5)                                                                        | Review (L6–L7)                               | Pipeline (L7+)                                                       |
| :----------------------- | :------------------------------ | :------------------------------------------------------------------------------------------ | :------------------------------------------- | :------------------------------------------------------------------- |
| `honest-epistemics`      | `ida` Stop, `pkb.nudge`         | `release_task` evidence fields                                                              | `rbg`, `marsha`                              | `enforcer-status`                                                    |
| `exercise-authority`     | AXIOMS weight (L1)              | `ida` AskUserQuestion nudge                                                                 | `rbg`, `marsha`, `qa` gate                   | `enforcer-status`                                                    |
| `halt-on-failure`        | `.agents/CORE.md`               | `handover` gate, `policy_enforcer`, deny rules                                              | `rbg`                                        | pre-commit, branch-protection AND-gate                               |
| `data-boundaries`        | `.agents/CORE.md`               | `sentinel` gate, credential isolation, deny rules                                           | `rbg`                                        | pre-commit, merge block                                              |
| `evidence-immutable`     | —                               | `sentinel` gate, records-path deny                                                          | `rbg`                                        | merge block                                                          |
| `single-source-of-truth` | `pkb.nudge`                     | `find_duplicates` (advisory)                                                                | `rbg`                                        | build asserts (import-resolve, decoy, agy-router), `enforcer-status` |
| `do-one-thing`           | `hydration.warn`, TodoWrite     | `enforcer` threshold                                                                        | `rbg`                                        | PR push warn                                                         |
| `categorical-imperative` | aops pre-impl phase             | —                                                                                           | `rbg`                                        | `enforcer-status`                                                    |
| `cite-sources`           | —                               | `/learn` RCA-schema block                                                                   | `rbg`                                        | `enforcer-status`                                                    |
| `judgment-non-delegable` | premise judgment on `→ queued`  | **premise gate** (`/pull`/`/dispatch`/`/supervisor` hard-refuse)                            | premise test (rbg + pauli + `/verify`)       | review backstop                                                      |
| **all axioms**           | AXIOMS weight (rbg only)        | `enforcer` gate (every N calls)                                                             | `enforcer`/`rbg` subagent, `rbg-review` Stop | `enforcer-status` AND-gate                                           |
| `single-source-of-truth` | `junior.md` pointer (always-on) | **related-work cohesion: ONE shared draft PR per coupled task set** (`cohesive-pr-epic.md`) | `rbg`                                        | `enforcer-status`                                                    |

> Project-local rules (`.agents/rules/RULES.md`) are enforced the same way: `rbg` (local + GHA `enforcer-status`) and `qa.agent` (GHA `qa-status`, Process-Compliance dimension) cite violations by `{#slug}`. The least-privilege **agent `tools:` allowlist** (`junior.md` + `ida.md` — no graph-mutation verbs in either; Ida adds `mcp__outlook__*`/`mcp__zot__*` domain tools for the academic-research role; graph-shaping routes to pauli via `/planner`, the sole graph-shaper) is a static L4 chokepoint enforcing `exercise-authority` (junior PR #1824; Ida D6).

---

## 3. Pyramid placement register (L0–L7)

The register-of-record for **where each mechanism sits** (escalation/coercion axis; width = volume × frequency). PRs that add/escalate/remove a mechanism cite its position and justify it against [`enforcement.md`](enforcement/enforcement.md) §4.1. The **cost axis** (coercion ≠ cost; the auto-mode-classifier and chokepoint rungs) is design reasoning and lives in [`enforcement.md`](enforcement/enforcement.md) §4.0 — not here.

| L  | Class      | Mechanism categories                                                                                                                                                                           | Per-fire cost               |
| -- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------- |
| L0 | base       | Authoring memory: PKB notes · comments · doc-strings                                                                                                                                           | ~0                          |
| L1 | base       | SessionStart guaranteed reads: persona · status strip · `session_env_setup` · `.agents/CORE.md` (`@`-import). **NOT** `AXIOMS.md` (rbg-only)                                                   | ~50–500 tok/session, cached |
| L2 | base       | Lifecycle context injection: UPS hints · hydrator · Stop-hook reminders (ida, qa-gate, handover-gate)                                                                                          | ~50–500 tok/event           |
| L3 | base       | Voluntary skill invocation: `/verify` · `/design-rubric` · `/q` · `/pull` · `/dispatch` · `/learn`                                                                                             | per-skill, agent decides    |
| L4 | middle     | Mechanical checks (deterministic): pre-commit hooks · bridge guards · `aca_data_autocommit` · `policy_enforcer` hard-deny · settings deny rules · **least-privilege chokepoint** (last-resort) | 50ms–2s                     |
| L5 | middle     | Judgment per-action gate (LLM): auto-mode classifier — `soft_deny`/`hard_deny`. **Narrow reserved / measurement-gated** (see Cost axis in enforcement.md)                                      | ~20–800 tok/fire + latency  |
| L6 | tip        | LLM-mediated review subagent: `rbg` · `marsha` · `enforcer` subagent · `alignment`                                                                                                             | ~1.5–3k tok + 5–30s         |
| L7 | tip (apex) | Branch protection + merge AND-gates: `<agent>-status` checks · `loop_detector` · human admit approval                                                                                          | merge-blocking              |

> **Base-tier note.** L1/L2/L3 are **delivery channels**; "escalating" a base-tier failure means walking the within-class insistence/placement spectrum (louder → reasoned → relocated → propagated → structured) _before_ crossing to a heavier class. Default to instructions; bias hard against new L5+ gates. See [`enforcement.md`](enforcement/enforcement.md) §4 and [`aops-core/skills/aops/references/enforcement-design.md`](../aops-core/skills/aops/references/enforcement-design.md).

---

## 4. PR / merge pipeline (current state)

The mechanisms that fire on a PR / at merge. Terse current-state rows; the canonical contract is [`workflows/pr-pipeline.md`](workflows/pr-pipeline.md), per-agent detail is in [`enforcement-mechanisms.md`](enforcement/enforcement-mechanisms.md) §L9–L10.

| Mechanism                  | Level | Required at merge?          | What it does                                                                                                                                                                                                                                                                                                                                               |
| :------------------------- | :---- | :-------------------------- | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `enforcer-status` (rbg)    | L6    | **yes**                     | LLM axiom review of HEAD diff; auto on PR events, manual `/enforce`. Blocks on CHANGES_REQUESTED/failure.                                                                                                                                                                                                                                                  |
| `qa-status` (marsha)       | L6    | **yes**                     | Runtime verification ("broken until proven"); auto on PR events, manual `/qa`/`/verify`.                                                                                                                                                                                                                                                                   |
| `admit-status` (human)     | L7    | **yes**                     | The single human "good idea — make it mergeable" approval (a maintainer PR review approval, `admit-on-review.yml`). Arms `--auto --squash`.                                                                                                                                                                                                                |
| Lint / Pytest              | L4    | **yes**                     | Mechanical CI.                                                                                                                                                                                                                                                                                                                                             |
| `alignment-status` (pauli) | L6    | no — **advisory by design** | PKB design-intent review (NOT axioms). Queue surface live; host dispatch spec-only. Informs the human admit gate.                                                                                                                                                                                                                                          |
| `responder-status` (§3.8)  | L4    | no — informational          | Pre-admission mechanical fix: typos, failing CI (including PR-attributable `Pytest` red — `check-mechred needs: [..., pytest]`, `PYTEST_RESULT` injected; base-broken guard prevents herd on shared failures, #1965). NOT merge conflicts — a conflicting PR never reaches it (§3.11). `MAX_RESPONDER_RUNS=3`; judgment calls surface to human unmodified. |
| `mechanic-status`          | L4    | no — informational          | Post-admission dev + conflict-resolve (the ONLY conflict resolver; a conflicting PR is reached only via the conflict-admission sweep — `conflict-admission-sweep.yml`, §3.11); `MAX_MECHANIC_RUNS=5`; enforcer+qa re-verify each SHA.                                                                                                                      |
| `review-attestation`       | L7    | in-ruleset (apply pending)  | Fail-closed proof a _named_ reviewer ran on _this_ SHA.                                                                                                                                                                                                                                                                                                    |
| branch protection          | L7    | **yes**                     | AND-gates the required checks (active ruleset `13762049`, API-verified 2026-06-09).                                                                                                                                                                                                                                                                        |

> Local/off-pipeline routes to the same agents: `/strategic-review` → james (deploys rbg+pauli+marsha, reconciles APPROVE/REVISE/ESCALATE, advisory); `/verify` → marsha; `/enforce` PR comment → rbg.

---

## Known gaps

- **Axioms are enforced reactively, not delivered always-on** (corrected 2026-06-03). No hook injects the axiom set at SessionStart; no working agent `@`-imports `AXIOMS.md` — only `rbg` does (as its review reference, not "told to obey"). `.agents/CORE.md` restates `halt-on-failure`/`data-boundaries` operationally, and (D1, spec [[mem-438429c5]] §4.1) now carries the **universal safety floor** — Safety Invariants (no credential read/store/broker) + PKB-HALT — as the one injected copy for every surface. **Injection-path caveat (D2 verify gate):** `session_env_setup.py` reads `aops-core/CORE.md`, which does NOT exist in-repo, so in dev/local sessions CORE.md reaches the model via the `@`-import in `CLAUDE.md`/`GEMINI.md`, not that hook read. Confirming the safety floor actually reaches a **polecat** requires verifying WHICH path carries CORE.md into the container and OBSERVING the prose in a real polecat transcript — the D2 hard gate that must be GREEN before `junior.md`'s per-agent safety copy is removed (D3). Enforcement is the periodic enforcer gate + the per-rule rows in [§2](#2-rule--mechanism-matrix). QA tracker: `aops-98c7ce49`.
- **Auto-mode classifier wired but unseeded** (L5). `autoMode` rules absent by deliberate 2026-06-04 decision — instruction tier walked first; a rule is seeded only on measurement showing instruction insufficient. Canonical: [`auto-mode-classifier.md`](enforcement/auto-mode-classifier.md).
- **Hydration / commit gates**: env vars wired (`HYDRATION_GATE_MODE`, `COMMIT_GATE_MODE`) but no `GateConfig` body — stubs.
- **Evidence loop steps 4–5** (`/aops` pattern detection → recommendation) aspirational; capture (step 2 `/learn`) and implementation (step 6) exist. Step 7 auto-map-update is manual.
- **`orchestrator-boundary-warn.md`** — template file not in `TEMPLATE_SPECS`; likely dead/unwired (cleanup follow-up).
- **PTY harness coverage** — measures Claude user-visibility + Stop agent-context; agy wire-acceptance, `--resume` persistence, and a model-echo agent-context lane for UPS/PreToolUse are not yet ported (`scripts/pty_hook_probe.py` is `client`-parameterized for it).
