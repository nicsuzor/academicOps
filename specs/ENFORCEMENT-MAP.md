---
title: Enforcement Map
type: state
category: state
permalink: state-enforcement-map
description: SSoT for the current-state register mapping rules to enforcement mechanisms, triggers, and gate modes across interactive/polecat/subagent surfaces.
---

# Enforcement Map

> **State — operative current-state register.** Which **rule** is reinforced by which **mechanism**, at which pyramid level, _right now_. This file is the single source of truth (SSoT) for the macroscopic matrix mapping rules to enforcement triggers, client differences, and gate modes.
>
> **Where things live now (read this if you came looking for prose):** each mechanism's canonical definition — what it is, what it enforces, how it's configured, its pyramid tier — is in [`enforcement/enforcement.md`](enforcement/enforcement.md) §6 (the authoritative mechanism index, aops-3038d47c). This file only records **WHEN** each mechanism fires; it does not redefine mechanisms. Per-gate forensic detail (config / verify / debug) is in [`enforcement/GATES.md`](enforcement/GATES.md). Per-agent detail is in `specs/agents/*.md`. Hook wire-channel detail (which field reaches user vs agent) is in [`CLIENT-TRANSLATION.md`](CLIENT-TRANSLATION.md). `rbg` blocks on this file's currency (P#65).

**Module ownership (H13/H16/H11/H17).** The gate engine, every surviving gate, and observability (`unified_logger`, the transcript family) stay in **aops-core** — aops-pkb is outer to the task contract, gates are inner (H13/H16). Ownership is not the same as deployment: this tooling must run wherever work actually happens, **including remote surfaces**, not just the primary host (H16). The skills-routing hint moves up to aops-pkb/aops-adhd; `pkb-nudge` does not (H11/H14). Git-push safety becomes project-local git hooks installed at provisioning, not plugin code — `aops-core`'s `git_safety.py` retires (H17). See [`enforcement.md` §6.1](enforcement/enforcement.md#61-live-mechanisms) for the full mechanism-by-mechanism ownership detail.

---

## 1. Unified SSoT Matrix: Rules, Mechanisms, and Triggers

This matrix is the centralized authoritative register tracking every core rule, how it is enforced (the mechanism — canonical definition in [`enforcement.md` §6](enforcement/enforcement.md#6-mechanism-index-authoritative)), when it fires (trigger), and its mode across interactive vs. autonomous surfaces. **Design intent is client-agnostic** — per-client wire differences are not behavioural intent and never appear here as rows or columns; they live as capability caveats in [`CLIENT-TRANSLATION.md`](CLIENT-TRANSLATION.md). (The Mode columns below are _surface_ distinctions — interactive vs. background vs. subagent — not clients.)

| Rule (Axiom/Directive)                                 | Mechanism / Gate                                                                                          | Trigger                                                                    | Level         | Mode: Interactive (`ida` surface) | Mode: Polecat (Background) | Mode: Subagents       | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| :----------------------------------------------------- | :-------------------------------------------------------------------------------------------------------- | :------------------------------------------------------------------------- | :------------ | :-------------------------------- | :------------------------- | :-------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `all axioms`                                           | `enforcer`/`rbg` gate                                                                                     | **PreToolUse:** every N (**~17 default**, was 50 — H2) write ops           | L4→L6         | `warn` (default)                  | `warn` (default)           | `warn` (default) — H8 | Fires uniformly for subagents/workers too (H8/H12 — no more `is_subagent` PreToolUse skip; see note below table). Subagent dispatch (the parent's `Agent(...)` call) still resets the parent's threshold.                                                                                                                                                                                                                                          |
| `all axioms`                                           | `rbg-review` gate                                                                                         | **Stop:** while task is bound                                              | L2→L6         | OPEN (inert)                      | `block`                    | `block`               | Held `CLOSED` until `rbg` runs. Armed by default; posture expressed **only** via env vars / `polecat.yaml` — no on/off/session-type/state code in the repo (H3). Suppressed while the session is **paused** — waiting on a background `subagent`/`workflow`/`monitor` or a `session_cron` (Claude Code 2.1.145+; see `_dispatch_gates`).                                                                                                           |
| `exercise-authority`, show-don't-tell                  | `qa` gate                                                                                                 | **Stop:** after task claimed `in_progress`                                 | L2            | `warn`                            | `warn`                     | `warn`                | Machinery unchanged (H10/H12 — reorganisation, not rewrite). `warn` DENYs once (forces one continuation) then latches open. Reopens when `qa`/`verify`/`marsha` runs. Suppressed while the session is **paused** (see `rbg-review`).                                                                                                                                                                                                               |
| `halt-on-failure`                                      | `handover` gate                                                                                           | **Stop:** if work was done (task/write tool)                               | L2            | `warn` (hard-block-once)          | `block`                    | `warn`                | Machinery unchanged (H10/H12). `warn` DENYs once (forces one continuation) then latches open. Reopens when `/end-session`, `/dump`, or `/continue` (pause — work in progress, task not concluded) runs. Suppressed while the session is **paused** (see `rbg-review`).                                                                                                                                                                             |
| Judgment/accountability — no work without a bound task | Task-binding gate — **REACTIVATED** (H4)                                                                  | **Tool call:** first mutating tool use before `claim_task`                 | L2/L4         | `block` (target)                  | `block` (target)           | `block` (target)      | Invariant: one session claims exactly one task (possibly its subtasks). Wiring is target-state — lands with the mechanics-separation task (aops-5b9e95c4), not this PR.                                                                                                                                                                                                                                                                            |
| `honest-epistemics`, `single-source-of-truth`          | `pkb.nudge`                                                                                               | **UserPromptSubmit**                                                       | L2            | advisory                          | n/a                        | advisory              | Stays at the lowest layer (aops-core); fires everywhere, incl. subagents (H5/H14).                                                                                                                                                                                                                                                                                                                                                                 |
| `do-one-thing`                                         | Skills-routing hint (`hydration.warn`)                                                                    | **UserPromptSubmit:** main session only                                    | L2            | advisory                          | n/a                        | advisory              | Ownership **moves up to aops-pkb/aops-adhd** (H11) — target state; aops-core's router stops owning this injection (lands with aops-5b9e95c4).                                                                                                                                                                                                                                                                                                      |
| Act-don't-surface (Nic-directed, A15 `aops-fef39347`)  | Task-notification guidance (`task_notification.guidance`)                                                 | **UserPromptSubmit:** task-notification prompts only                       | L2            | advisory                          | advisory                   | advisory              | Background-completion notifications get ONE guidance injection (act on it; don't relay routine noise to the user) where the path previously returned zero output. Gates, hydrator, and `pkb.nudge` all still skip these prompts — this is the only thing added on that branch. Same mechanism class as `pkb.nudge`/`hydration.warn`. Added by PR #2139.                                                                                            |
| `halt-on-failure`, `data-boundaries`                   | Safety floor (`CORE.md`)                                                                                  | **SessionStart:** `@`-import                                               | L1            | instruction                       | instruction                | instruction           | One injected copy for every surface, via the `@`-import only — `session_env_setup.py`'s hook-level attempt at this is retired (H9).                                                                                                                                                                                                                                                                                                                |
| `single-source-of-truth` (related-work cohesion)       | `enforcer`/`rbg` review + `cohesive-pr-epic.md`                                                           | **PR-time:** a coupled task set must share ONE draft PR                    | L7            | review                            | review                     | review                | Always-on rule.                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `honest-epistemics`                                    | Evidence contract — PKB tool boundary (`release_task`/`complete_task`)                                    | **Tool call:** task release/completion                                     | L2            | enforced                          | enforced                   | enforced              | **Primary enforcement point** (H7): a task cannot be released without completion evidence or a stated failure reason. `summary` required on release; `pr_url` (if given) live-verified via `gh` before acceptance — the anti-fabrication integrity gate (`polecat/pkb_bridge.py`). Framed to agents as "land the plane": commit → push → `release_task`, or the work is garbage-collected (H10) — incentive-first, this machinery is the backstop. |
| `judgment-non-delegable`                               | [premise gate](enforcement/premise-gate.md) — `rbg` + `pauli` clearance via `/strategic-review --premise` | Spend surfaces (`/pull`, `/dispatch`, `/supervisor` dispatch), pre-compute | dispatcher L4 | `block`                           | `block`                    | `block`               | Two-judge agent clearance, not a hook: dispatcher ensures a legible premise, then `rbg` (axiom/rig) + `pauli` (worth/shape) each return CLEAR/BOUNCE; any BOUNCE hard-refuses. Reuses the `/strategic-review` judges (DRY); the same judgment re-fires on completed work at the review surface. Enforcement attaches at dispatch, not at the hookless manual `→ queued` transition.                                                                |

> **`sentinel` gate: DELETED (H1).** Destructive-verb + protected-path PreToolUse matching failed the "no shitty NLP" rule; the concern (accidental damage to the live environment) is operationalised by container isolation instead. **`ida` gate: retired as a hook (H6)** — the Stop honesty check and the `AskUserQuestion` nudge both defer to the head-personality surface (`ida`, `aops-core`) interacting with the human, not a router-level gate. See [`enforcement.md` §6.2](enforcement/enforcement.md#62-retired--archived-h1h18) for the full retired-mechanism disposition.
>
> **Column-header note:** the "Mode: Interactive (`ida` surface)" column in the table above names the interactive-client surface (`ida`, `aops-core` — talking to the human; the short-lived `aops-interactive` plugin that briefly held `ida`/`junior` was dissolved pre-ship per ruling A10, aops-7ea63b63), not the retired `ida` gate (H6) documented immediately above — the two are unrelated uses of the same name.
>
> **Subagent surface, precisely (H8/H12):** gates now fire **uniformly** across main sessions, subagents, and workers — the previous `is_subagent` skip of `PreToolUse`/`PostToolUse` evaluation is **retired**. With `sentinel` gone, `enforcer`/`rbg` is the only PreToolUse-triggered gate remaining, and it now evaluates on a subagent's/worker's own tool calls the same as on the main session's. This is pursued as a reorganisation of the existing per-session `GateState` design (H12), not a rewrite to full statelessness. The `AOPS_AGY_CLIENT=1` worker-posture override, which previously forced `is_subagent=True` to get the same skip, is moot for gating purposes now that the skip itself is gone — it may persist for session-type observability labelling only. Forensic detail: [`enforcement/GATES.md`](enforcement/GATES.md) § Subagent & worker session scope.

### 1.1 Per-message routing (agent-first)

Every hook message is enforcement **directed at the agent** — the **agent channel is non-negotiable and always present** (the `Agent template` column, which is the `CONTEXT_INJECTION` template the agent reads). What is _optional_ is whether anything also reaches the **user terminal**:

- **`silent`** — nothing rendered to the user; the agent handles it.
- **`same`** — the user sees the same text the agent sees (no separate template).
- **`[[file]]`** — a user-specific `USER_MESSAGE` template (the linked `*.policy_message` file).

This table owns the **disposition** (who-sees-what); wire-mechanics and per-client capability detail live in [`CLIENT-TRANSLATION.md`](CLIENT-TRANSLATION.md).

| Gate · fire (event)                             | Agent template (always)                                                                  | User message                                                                        | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| :---------------------------------------------- | :--------------------------------------------------------------------------------------- | :---------------------------------------------------------------------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `enforcer` · block (PreToolUse)                 | [rbg-policy-context](../aops-core/hooks/templates/rbg-policy-context.md)                 | `silent`                                                                            | Periodic compliance plumbing; outcome + hook in transcript only. Fires uniformly incl. subagents/workers (H8).                                                                                                                                                                                                                                                                                                                                     |
| `enforcer` · countdown (PreToolUse run-up)      | —                                                                                        | `silent`                                                                            | "N calls until the check" is pure user noise.                                                                                                                                                                                                                                                                                                                                                                                                      |
| `enforcer` · dispatch (PreToolUse)              | [rbg-instruction](../aops-core/hooks/templates/rbg-instruction.md)                       | `silent`                                                                            | Instruction to invoke the enforcer subagent.                                                                                                                                                                                                                                                                                                                                                                                                       |
| `qa` · block (Stop)                             | [qa-policy-context](../aops-core/hooks/templates/qa-policy-context.md)                   | `silent`                                                                            | "Verify before you stop" is agent-directed. Machinery unchanged (H10/H12).                                                                                                                                                                                                                                                                                                                                                                         |
| `handover` · block (Stop)                       | [stop-gate-handover-block](../aops-core/hooks/templates/stop-gate-handover-block.md)     | `silent`                                                                            | Agent-directed handover instruction. Machinery unchanged (H10/H12).                                                                                                                                                                                                                                                                                                                                                                                |
| `rbg_review` · block (Stop)                     | [rbg-policy-context](../aops-core/hooks/templates/rbg-policy-context.md)                 | `silent`                                                                            | Polecat/crew-only, agent-directed.                                                                                                                                                                                                                                                                                                                                                                                                                 |
| block-gate · degraded escape-hatch (Stop)       | —                                                                                        | `keep` ([rbg-review-degraded](../aops-core/hooks/templates/rbg-review-degraded.md)) | Mechanism-class row: every block-mode Stop gate (`rbg_review`@5, plus `qa`/`handover`@engine-default 3) downgrades `DENY`→`WARN`-and-allow after N consecutive unsatisfied Stops in a turn (per-turn counter, resets on UPS). Loud **by design** — the user **should** see it when a gate gives up. Replaces the deleted router `stop_hook_active` bypass as the client-agnostic loop guard (with the 5-blocks-in-2-min override as residual net). |
| `pkb` · nudge (UserPromptSubmit)                | [pkb-nudge](../aops-core/hooks/templates/pkb-nudge.md)                                   | `silent`                                                                            | Already agent-only. Stays in aops-core, lowest layer, fires everywhere (H5/H14).                                                                                                                                                                                                                                                                                                                                                                   |
| `hydration` · routing hint (UserPromptSubmit)   | [hydration-gate-warn](../aops-core/hooks/templates/hydration-gate-warn.md)               | `silent`                                                                            | Routing hints are agent-directed context injection, not a user message. Ownership moves up to aops-pkb/aops-adhd (H11) — target state, aops-core stops owning this (lands with aops-5b9e95c4).                                                                                                                                                                                                                                                     |
| task-notification · guidance (UserPromptSubmit) | [task-notification-guidance](../aops-core/hooks/templates/task-notification-guidance.md) | `silent`                                                                            | Fires only on background task-notification prompts (`_is_task_notification`); gates/hydrator/nudge all still skip these. Agent-directed: act on the notification, surface to the user only if important or awaited (A15 act-don't-surface ruling, `aops-fef39347`; PR #2139).                                                                                                                                                                      |

**Status / transition pings** (`allow` verdict — informational, not enforcement). Default `silent`; the agent already knows the gate cleared:

| Ping                        | User message | Notes                                             |
| :-------------------------- | :----------- | :------------------------------------------------ |
| `enforcer.verified` (reset) | `silent`     | Counter reset — agent-internal.                   |
| `qa.complete`               | `silent`     | Verifier ran.                                     |
| `handover.bound`            | `silent`     | Task bound.                                       |
| `handover.complete`         | `keep`       | Session-end confirmation — user-useful, low-rate. |
| `rbg_review.complete`       | `silent`     | rbg ran, gate cleared.                            |

> **Out of scope:** `SUBAGENT_INSTRUCTION` templates (`enforcer.context`, `qa.context`, `enforcer.audit`, `rbg_review.context`) reach **neither** the user nor the main agent — they are written to the temp file a _dispatched_ subagent reads. No user/agent disposition applies.

---

## 2. Capability & Identity Restrictions (Tool Access)

Tool and identity restrictions serve as foundational enforcement mechanisms, restricting what agents can do based on their role.

| Restriction Area                          | Enforcement Rule                                                                                                                                                                                                                                                                                                                                                                                                             | Applied To                                                                                 |
| :---------------------------------------- | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :----------------------------------------------------------------------------------------- |
| **Specialized Execution (Playwright)**    | Playwright and heavy e2e testing tools are strictly gated to specialized validation agents (`marsha`), enforcing separation of authoring vs QA.                                                                                                                                                                                                                                                                              | Excludes: `ida`, `pauli`, `rbg`                                                            |
| **PKB Knowledge Write**                   | `ida` moved to the short-lived `aops-interactive` plugin (aops-cf3fb2f0) and then back to `aops-core` when that plugin was dissolved pre-ship (ruling A10, aops-7ea63b63); `pauli`, `james`, `marsha`, `rbg` moved to the `aops-pkb` plugin (aops-b225ec53); none of them own a PKB MCP server of their own, so all five use `mcp__plugin_aops-pkb_pkb__*`. Writes from that namespace are constrained by `data-boundaries`. | Enforced via `rbg` review & `data-boundaries`                                              |
| **Baseline Tooling**                      | Universal access to `Bash`, `Edit`, `Grep`, `Read`, `Skill` is granted across interactive (`ida`) and background workers to ensure `full-observability`.                                                                                                                                                                                                                                                                     | All Authoring Agents                                                                       |
| **Command-Level Skill-Only Funnel**       | A slash-command's frontmatter `allowed-tools` is narrowed to `Skill` only, forcing every execution path through the delegated skill so the command file stays a thin wrapper with no independent tool surface (a chokepoint/funnel, L4).                                                                                                                                                                                     | `dispatch.md`, `maintain.md`, `pull.md`                                                    |
| **Comment-Triggered Agent Identity Gate** | An `@claude` mention that would grant write/Bash access is restricted to `COLLABORATOR`/`MEMBER`/`OWNER` comment/review authors (`author_association`), matching `trigger-enforcer.yml`/`trigger-qa.yml`'s allowlist — an arbitrary commenter cannot invoke a write-capable agent.                                                                                                                                           | `claude.yml` (`claude` job only — `claude-issue-triage` is read-only regardless of author) |

> **Note on Voluntary Skills (L3)**: Voluntary skills (e.g., `/verify`, `/design-rubric`, `/q`) are **capabilities, not enforcement mechanisms**. The restrictions on their use, and the hooks (like `qa` gate) that remind agents to use them, constitute the actual enforcement.

### Local mechanical checks (pre-commit hooks)

Previously missing from this catalogue (flagged by Nic against the pre-redraft `enforcement.md`, aops-3038d47c). Canonical definition: [`enforcement.md` §6.1](enforcement/enforcement.md#61-live-mechanisms).

| Mechanism                     | Level | Required at commit? |
| :---------------------------- | :---- | :------------------ |
| Pre-commit mechanical checks  | L8    | **yes**             |
| Project-local git-push safety | L8    | **yes** (target)    |

---

## 3. PR / merge pipeline (current state)

The mechanisms that fire on a PR / at merge. Terse current-state rows; the canonical contract is [`workflows/pr-pipeline.md`](workflows/pr-pipeline.md), per-agent detail is in `specs/agents/*.md`.

| Mechanism                             | Level | Required at merge?          | What it does                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| :------------------------------------ | :---- | :-------------------------- | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `enforcer-status` (rbg)               | L6    | **yes**                     | LLM axiom review of HEAD diff; auto on PR events, manual `/enforce`. Blocks on CHANGES_REQUESTED/failure — but only for violations left **unresolved**; a violation the agent found and fixed itself in the same pass (committed) never blocks, even though the fix commit means the posted verdict lands on the next SHA, not this one (pr-pipeline §3.4 pt 6, §4.2; `scripts/ci/enforcer-terminal-status.sh`). Known open failure mode: if the PR's own author identity collides with the agent's (`claude[bot]`), `gh pr review` is rejected as a self-review and the check sticks at failure until a human intervenes — root cause is credential isolation (`aops-ae3aa475`), not this pipeline; no automated recovery is attempted (pr-pipeline §4.2).                                                                                            |
| `qa-status` (marsha)                  | L6    | **yes**                     | Runtime verification ("broken until proven"); auto on PR events, manual `/qa`/`/verify`. Terminal status is decided by `scripts/ci/qa-terminal-status.sh`, whose review lookup is scoped to marsha's `# QA Verification` marker on HEAD — an approval from another agent (e.g. the enforcer) on the same SHA does **not** green it, and an absent QA verdict fails closed (aops_e958bd56). Same self-review identity-collision failure mode as `enforcer-status` above (pr-pipeline §4.2; `qa.agent.md`).                                                                                                                                                                                                                                                                                                                                              |
| `admit-status` (human)                | L7    | **yes**                     | The human "good idea — make it mergeable" decision: `admit-on-review.yml` for normal PRs; `conflict-admission-sweep.yml` (deferred execution of the standing approval) for conflicting PRs that cannot receive `pull_request_review` events. Both call the same `scripts/ci/admit-pr.sh` (POST admit-status + arm `--auto --squash`) and the same `scripts/ci/reviewer-authz.sh` (write-class-or-allowlisted check) so the grant and the policy can't drift between the two origins (pr-pipeline §5.1). **Sticky**: once admitted, carries forward across every push until a terminal state (merge / §3.6 exhaustion / §3.10 changes-requested); a push never re-judges admission (pr-pipeline §5).                                                                                                                                                    |
| Lint / Pytest                         | L4    | **yes**                     | Mechanical CI.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `alignment-status` (pauli)            | L6    | no — **advisory by design** | PKB design-intent review (NOT axioms). Queue surface live; host dispatch spec-only. Informs the human admit gate.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `responder-status` (§3.8)             | L4    | no — informational          | Pre-admission mechanical fix: typos, failing CI (including PR-attributable `Pytest` red — `check-mechred needs: [..., pytest]`, `PYTEST_RESULT` injected; base-broken guard prevents herd on shared failures). NOT merge conflicts — a conflicting PR never reaches it (§3.11). `MAX_RESPONDER_RUNS=3`; judgment calls surface to human unmodified.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `mechanic-status`                     | L4    | no — informational          | Post-admission dev + conflict-resolve (the ONLY conflict resolver; a conflicting PR is reached only via the conflict-admission sweep — `conflict-admission-sweep.yml`, §3.11); `MAX_MECHANIC_RUNS=5`; enforcer+qa re-verify each SHA. Terminal `failure` (the per-SHA loop guard) is written **only** on §3.6 ceiling exhaustion, which also posts an escalation review; a harness crash/timeout instead writes a **non-terminal, retryable `error`** with no review, so the crashed SHA is retried on the next pass rather than trapped (aops-c5c57bfd, Defect 2). The sweep dispatches the mechanic under `schedule`/`workflow_dispatch`; a `push`-triggered sweep re-dispatches itself first, since `claude-code-action@v1` aborts on an inherited `push` context (Defect 1). Reset a terminal SHA in place via the `mechanic-retry` label (below). |
| `claude.yml` (`@claude` mention)      | L4    | no — on-demand assistant    | Unattended dev assistant triggered by an `@claude` mention (PR review comment, PR review, or PR-scoped issue comment) or manual `workflow_dispatch`; same capability class as `mechanic-status`: `contents: write`, PAT-backed full-history checkout, `--allowed-tools "Bash,Read,Edit,Write"` (added PR #2078 — closed a gap where it could diagnose a stuck PR, e.g. a merge conflict, but not fetch/rebase/push a fix). Gated to `COLLABORATOR`/`MEMBER`/`OWNER` triggers (§2). A bare-issue `@claude` mention runs the separate `claude-issue-triage` job instead — read-only, no PAT, no `--allowed-tools` — because a plain issue has no PR head branch to scope a checkout to (it would land on the default branch). Posts no status; never gates merge.                                                                                        |
| `review-attestation`                  | L7    | in-ruleset (apply pending)  | Fail-closed proof a _named_ reviewer ran on _this_ SHA. On failure, writes a cause+remedy breakdown to the Actions run summary (points at Force Review, pr-pipeline §3.7).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `comment-triage-status`               | L6    | in-ruleset (apply pending)  | Fail-closed gate over unresolved third-party review comments/reviews (Copilot, Gemini, ...) — restores the v1 merge-prep invariant ("read ALL reviews", F5) that PR #2094 exposed as silently dropped when merge-prep was retired at Phase 5. Runs on every push (`pr-pipeline.yml`) and at admission (`admit-on-review.yml`'s `admit-comment-triage`); also feeds `decide-mechanic`'s dispatch, which sends the mechanic in `review-response` mode when this is the only red signal (`scripts/ci/check-unresolved-comments.sh`, unit-tested).                                                                                                                                                                                                                                                                                                         |
| branch protection                     | L7    | **yes**                     | AND-gates the required checks (active ruleset `13762049`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| CODEOWNERS review request             | L2    | no — surfacing              | `.github/CODEOWNERS` (`* @nicsuzor`) auto-requests the maintainer's review on PR open, routing PRs to the L7 human admit gate. Request-only; NOT wired as a "require code-owner review" branch-protection rule (pr-pipeline §3.2).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| Force Review (escape hatch)           | L4    | no — manual re-trigger      | `force-review.yml` (`workflow_dispatch`) re-runs the enforcer/qa merge-gate reviewers on a wedged PR's HEAD via the same reusable workflows. Bypasses admission ON PURPOSE (does not merge — merge still needs all required checks green + `admit-status`); sibling of `/enforce`·`/qa` manual triggers (pr-pipeline §3.12).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `mechanic-retry` label (escape hatch) | L4    | no — manual reset           | Maintainer-added PR label that clears a **terminal** per-SHA `mechanic-status` (§3.6 exhaustion) in place and re-runs the mechanic under the standing approval, **without a new commit**. Handled by `conflict-admission-sweep.yml`'s `discover` job (posts `pending` to clear the guard, removes the label, then the existing sweep re-includes the SHA and re-dispatches). Label-as-authorization: only write collaborators can label a PR. The one reset channel that also works while the PR is `CONFLICTING` (no `pull_request`/`_review` event fires there). Does not merge — merge still needs all required checks + `admit-status` (aops-c5c57bfd, pr-pipeline §3.6/§3.11).                                                                                                                                                                    |

> Local/off-pipeline routes to the same agents: `/strategic-review` → james (deploys rbg+pauli+marsha, reconciles APPROVE/REVISE/ESCALATE, advisory); `/verify` → marsha; `/enforce` PR comment → rbg.

### Post-hoc pattern review (evidence loop, not per-PR)

Previously missing from this catalogue (flagged by Nic against the pre-redraft `enforcement.md`, aops-3038d47c). Fires on a user-set cadence, not per-PR; canonical definition and the witness/judge split rationale: [`enforcement.md` §5](enforcement/enforcement.md#5-evidence-loop).

| Mechanism                              | Level | Required?     |
| :------------------------------------- | :---- | :------------ |
| `/issue-sweep` post-hoc pattern review | L11   | no — periodic |

---

## Known gaps

- **Axioms are enforced reactively, not delivered always-on.** No hook injects the axiom set at SessionStart; no working agent `@`-imports `AXIOMS.md` — only `rbg` does (as its review reference, not "told to obey"). `.agents/CORE.md` restates `halt-on-failure`/`data-boundaries` operationally and carries the **universal safety floor** — Safety Invariants (no credential read/store/broker) + PKB-HALT — as the one injected copy for every surface. CORE.md reaches the model via the `@`-import in `CLAUDE.md`/`GEMINI.md` at SessionStart; `session_env_setup.py`, which used to also attempt a hook-level read of a non-existent `aops-core/CORE.md`, is retired (H9) — the `@`-import is now the only path, not a fallback. Coverage is verified per-surface: polecat workers' PKB-HALT floor is asserted by `tests/polecat/test_prompt_template.py::test_prompt_carries_pkb_halt_floor` (checks `PKB_HALT_FLOOR` reaches `build_polecat_prompt` output in both task and issue modes); interactive/in-repo-agent PKB-HALT coverage is the `.agents/CORE.md` prose itself (§ PKB-HALT), the one injected copy for that surface — no separate per-agent duplicate. Enforcement is the periodic enforcer gate plus the per-rule rows in [Unified Matrix](#1-unified-ssot-matrix-rules-mechanisms-and-triggers).
- **Auto-mode classifier seeded and populated** (L5). The polecat `autoMode` ruleset (`polecat/defaults/claude-settings.json`) carries `environment`/`allow`/`soft_deny` entries — the individual rules are maintained directly in that file, not enumerated per-rule here or in the canonical spec, per the mechanism-class grain this map tracks. New rules still follow the evidence loop before landing. Canonical: [`auto-mode-classifier.md`](enforcement/auto-mode-classifier.md).
- **Hydration / commit gates**: env vars wired (`HYDRATION_GATE_MODE`, `COMMIT_GATE_MODE`) but no `GateConfig` body — stubs.
- **Evidence loop step 5** (recommendation synthesis within `/issue-sweep`) still leans on agent judgment per cycle rather than a scored model; capture (step 2 `/learn`), pattern detection + classification (steps 3–4, `/issue-sweep`), and implementation (step 6) exist and are in active use. Step 7 auto-map-update is manual.
- **`orchestrator-boundary-warn.md`** — template file not in `TEMPLATE_SPECS`; likely dead/unwired (cleanup follow-up).
- **PTY harness coverage** — measures Claude user-visibility + Stop agent-context; agy wire-acceptance, `--resume` persistence, and a model-echo agent-context lane for UPS/PreToolUse are not yet ported (`scripts/pty_hook_probe.py` is `client`-parameterized for it).
