[project]
name = "aops-jr"
version = "0.0.1"
description = "aops-jr persona plugin — jr/ida personas and templates awaiting v0.5 face-extraction"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = []
