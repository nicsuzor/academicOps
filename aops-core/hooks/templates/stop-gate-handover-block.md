<academicOps stop hook warning>
▶ **Land the plane** before you release control back to the user. You MUST use the `end_session` skill to finalize this EPHEMERAL session (inline prose won't unlock the gate). Mutating tools after the skill will re-arm this gate.
</academicOps stop hook warning>
