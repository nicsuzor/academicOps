<aops-warning>▶ **Hand over before you stop.** This session did real work — invoke a skill to close it (inline prose won't unlock the gate):

- `/end-session` — done or terminally blocked. Commits, pushes, opens the PR, releases the task.
- `/dump` — mid-flight bail (context full). Captures a resume task only.

Then give your normal summary as your final message. (Mutating tools after the skill re-arm this gate.)</aops-warning>
