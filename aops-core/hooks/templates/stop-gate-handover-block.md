<aops-warning>Work is in progress. Invoke the skill — inline handover prose does not unlock this gate:

- `/end-session` — task done or terminally blocked. Full close: commit + push + PR + release_task + reflection.
- `/dump` — mid-flight bail (context full, needs restart). Captures resume task. No commit/PR/reflection.
  (Mutating tools used after the skill will reset this gate.)</aops-warning>
