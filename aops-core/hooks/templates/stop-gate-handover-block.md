<academicOps stop hook warning>▶ **Land the plane**. Do everything you need to do, make sure memories are recorded, curated; tasks updated; commits pushed; PRs filed. When your session ends, everything left uncommitted or not properly stored will be DESTROYED.

**Hand over before you stop.** This session did real work — invoke a skill to close it (inline prose won't unlock the gate):

- `/end-session` — done or terminally blocked. Commits, pushes, opens the PR, releases the task.
- `/dump` — mid-flight bail (context full). Captures status and a resume task only.

Mutating tools after the skill will re-arm this gate.

</academicOps stop hook warning>
