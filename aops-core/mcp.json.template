{
  "claude": {
    "mcpServers": {
      "pkb": {
        "type": "http",
        "url": "${user_config.PKB_MCP_URL}"
      }
    }
  },
  "gemini": {
    "mcpServers": {
      "pkb": {
        "command": "uvx",
        "args": ["fastmcp", "run", "${PKB_MCP_URL}"]
      }
    }
  }
}
