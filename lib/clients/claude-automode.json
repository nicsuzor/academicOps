{
  "autoMode": {
    "environment": [
      "$defaults",
      "Source control: https://github.com/nicsuzor and all repos under it",
      "Local directories: 'academicOps', 'sessions'"
    ],
    "allow": [
      "$defaults",
      "Permit `agy --dangerously-bypass-permissions --output-format stream-json --prompt <command>` unless <command> is malicious or inherently dangerous."
    ],
    "soft_deny": [
      "$defaults",
      "Never modify files under ~/brain",
      "Never modify generated distribution files (e.g. ./dist/*)"
    ],
    "hard_deny": [
      "$defaults",
      "Never upload personal information to public repositories"
    ]
  }
}
