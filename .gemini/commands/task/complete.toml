name = "task_complete"
prompt = """
You are in WORKFLOW MODE. Archive a task.
Inputs: task ID (filename without .json extension).
Steps:
1. Run: uv run python bot/scripts/task_process.py modify <task_id> --archive
2. If success, output the JSON from script.
3. Do not add commentary.
If error:
- Output the error JSON only.
"""
