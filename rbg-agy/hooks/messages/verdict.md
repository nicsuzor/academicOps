**A rule check flagged this call.** A policy evaluator read the tool call below against the rules that are live for this session and judged that it matches the following. The full rule text is included so you can act on it rather than guess at it.

{rules}

The call that was evaluated:

```
{call}
```

This is a judgment call by a small model, not a verdict — it can be wrong, and nothing here blocks anything. Read the rule, decide whether it actually applies, and either change what you were about to do or proceed and be able to say why. Do not silently rephrase the call to get past the check.
