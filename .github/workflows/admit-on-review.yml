# Admit on Review — the v2 PR pipeline's human admission gate.
#
# Admission to the Stage-2 fix loop is a maintainer's PR **review approval**.
# Clicking "Approve" on the PR is the single human decision of the pipeline:
# "this is a good idea; make it mergeable" (specs/workflows/pr-pipeline.md §3.2).
#
# This REPLACES the retired `pr-fix-loop` GitHub Environment deployment gate
# (and the in-pipeline `admit` job that parked on it). Two reasons it is better
# (see §3.2 "superseded" note + the PR #1858 evidence):
#   1. DISCOVERABILITY — the GitHub Environment approval surfaced only as
#      "Review deployments → Approve" buried inside an Actions run. The PR
#      "Approve" button is the most prominent control on the PR.
#   2. EVENT-DRIVEN DISPATCH — approving an Environment emits NO event, so the
#      mechanic that the admission is supposed to unleash never re-entered the
#      pipeline on the admitted SHA (the parked `admit` job and the mechanic's
#      `check-admit` dispatch gate raced inside one run; check-admit read
#      admit-status=pending and skipped the mechanic ~2 min before the human
#      approval set it to success — admission stranded). A `pull_request_review`
#      IS a workflow trigger, so this workflow re-enters the pipeline on the
#      admitted SHA and dispatches the mechanic directly. The race is gone with
#      the parked job.
#
# What stays unchanged: `admit-status` is still the required merge gate;
# sticky carry-forward (admission persists across every push until a terminal
# state, §5) lives in `pr-pipeline.yml`'s `initialize` job; the mechanic + its
# §3.6 bounded loop are owned by
# `agent-mechanic.yml`; passes 2…N of the fix loop are still dispatched by
# `pr-pipeline.yml`'s `check-admit → mechanic` jobs on the `synchronize` events
# that the mechanic's own commits fire.

name: Admit on Review

on:
  pull_request_review:
    types: [submitted]

permissions:
  contents: write
  pull-requests: write
  statuses: write
  id-token: write
  issues: write

concurrency:
  # Per-PR: a second approval for the same PR queues rather than racing a
  # running admission. cancel-in-progress: false — never kill an in-flight
  # admit/mechanic dispatch.
  group: admit-on-review-${{ github.event.pull_request.number }}
  cancel-in-progress: false

jobs:
  # ─── Authorise: is this approval an admission signal? (fail-closed) ────────
  # Only same-repo PRs (a fork-origin run cannot mint the bot write token) and
  # only `approved` reviews reach the decision script. The script default-denies
  # anything that is not a write-class maintainer approval.
  authorize:
    name: Authorize admission
    if: >-
      github.event.review.state == 'approved' &&
      github.event.pull_request.head.repo.full_name == github.repository &&
      github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    outputs:
      decision: ${{ steps.decide.outputs.state }}
      head_ref: ${{ github.event.pull_request.head.ref }}
      pr_number: ${{ github.event.pull_request.number }}
    env:
      GH_TOKEN: ${{ secrets.AOPS_BOT_GH_TOKEN || github.token }}
      REPO: ${{ github.repository }}
      REVIEWER_LOGIN: ${{ github.event.review.user.login }}
      REVIEW_STATE: ${{ github.event.review.state }}
      # ADMIT_ALLOWLIST is intentionally NOT set here — admit-on-review.sh
      # sources scripts/ci/reviewer-authz.sh, which supplies the single
      # default (pr-pipeline.md §5.1) shared by every admission path. Set it
      # here only to override the default for this call site specifically.
    steps:
      - uses: actions/checkout@v4
      - name: Resolve reviewer repository permission
        id: perm
        run: |
          PERM=$(gh api "repos/$REPO/collaborators/$REVIEWER_LOGIN/permission" \
            --jq '.permission' 2>/dev/null || echo "none")
          echo "Reviewer $REVIEWER_LOGIN permission: $PERM"
          echo "permission=$PERM" >> "$GITHUB_OUTPUT"
      - name: Decide admission
        id: decide
        env:
          REVIEWER_PERMISSION: ${{ steps.perm.outputs.permission }}
        run: ./scripts/ci/admit-on-review.sh

  # ─── Admit: set the required admit-status + arm auto-merge ─────────────────
  admit:
    name: Admit — set admit-status, arm auto-merge
    needs: [authorize]
    if: needs.authorize.outputs.decision == 'admit'
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      statuses: write
    outputs:
      # The SHA actually admitted (re-read live — the PR may have advanced since
      # the review was submitted). Re-verification + the mechanic target THIS SHA.
      admitted_sha: ${{ steps.resolve.outputs.sha }}
      # Whether the named reviewers are ALREADY green on the admitted SHA. 'true'
      # when the PR was admitted right after its ready-review with no intervening
      # pushes — auto-merge fires immediately, no re-verification needed. 'false'
      # when pre-admission pushes advanced HEAD past the last reviewed SHA (§3.1
      # fire-once gate skipped the reviewers on those pushes), so enforcer/qa/
      # attestation are absent here and the admission boundary re-fires them below.
      reviewers_green: ${{ steps.green.outputs.reviewers_green }}
    env:
      # Bot PAT required to write the required `admit-status` and arm auto-merge
      # (the default token cannot satisfy a required check posted as a status
      # the ruleset trusts; §4.7). No github.token fallback — fail closed.
      GH_TOKEN: ${{ secrets.AOPS_BOT_GH_TOKEN }}
      REPO: ${{ github.repository }}
      PR_NUMBER: ${{ needs.authorize.outputs.pr_number }}
    steps:
      - uses: actions/checkout@v4
      - name: Resolve live HEAD SHA
        id: resolve
        run: |
          set -euo pipefail
          # Re-read HEAD live: the PR may have advanced while/after the review
          # was submitted. Admit the CURRENT head, the honest state.
          HEAD_SHA=$(gh pr view "$PR_NUMBER" --repo "$REPO" --json headRefOid --jq '.headRefOid')
          echo "Admitting PR #$PR_NUMBER at HEAD $HEAD_SHA"
          echo "sha=$HEAD_SHA" >> "$GITHUB_OUTPUT"
      - name: Grant admission
        # Shared with conflict-admission-sweep.yml's admit job
        # (scripts/ci/admit-pr.sh, pr-pipeline.md §5.1) — the required
        # admit-status write + armed auto-merge can't drift between the two
        # admission paths. This step's own GH_TOKEN guard (fail closed if
        # unset) lives inside the shared script now.
        run: bash scripts/ci/admit-pr.sh
        env:
          SHA: ${{ steps.resolve.outputs.sha }}
          REASON: "Admitted by maintainer review approval — good idea, make it mergeable"
      - name: Check whether reviewers are already green on the admitted SHA
        id: green
        env:
          HEAD_SHA: ${{ steps.resolve.outputs.sha }}
        run: |
          set -euo pipefail
          # Are the named reviewers ALREADY green on the admitted SHA? Under the
          # §3.1 fire-once gate the reviewers run on ready + at admission, NOT on
          # pre-admission pushes — so an admitted SHA that advanced past the last
          # reviewed commit has ABSENT enforcer/qa statuses here. Classify so the
          # admission boundary re-fires the reviewers when they are not already green
          # (absent OR red). When both are already success, auto-merge handles the
          # merge and no re-verification or mechanic is needed.
          ENF=$(gh api "repos/$REPO/commits/$HEAD_SHA/statuses" \
            --jq '[.[] | select(.context == "enforcer-status")] | sort_by(.created_at) | last.state // ""' 2>/dev/null || echo "")
          QA=$(gh api "repos/$REPO/commits/$HEAD_SHA/statuses" \
            --jq '[.[] | select(.context == "qa-status")] | sort_by(.created_at) | last.state // ""' 2>/dev/null || echo "")
          echo "enforcer-status='${ENF:-absent}' qa-status='${QA:-absent}' on $HEAD_SHA"
          if [ "$ENF" = "success" ] && [ "$QA" = "success" ]; then
            echo "Both reviewers already green on the admitted SHA — auto-merge handles it."
            echo "reviewers_green=true" >> "$GITHUB_OUTPUT"
          else
            echo "Reviewers not green on the admitted SHA — admission boundary will re-fire them."
            echo "reviewers_green=false" >> "$GITHUB_OUTPUT"
          fi

  # ─── Admission-boundary re-verification (§3.1 fire-once + §3.7 attestation) ──
  # The §3.1 fire-once gate skips enforcer/qa on pre-admission pushes, so the
  # admitted SHA can lack the REQUIRED enforcer-status / qa-status / review-attestation
  # (§7). These jobs re-fire the named reviewers ON THE ADMITTED SHA and recompute
  # the attestation, so the required checks resolve and auto-merge can fire — for an
  # already-clean PR — WITHOUT depending on the mechanic pushing a commit (the
  # deadlock the naive gate would create). They run ONLY when the reviewers are not
  # already green on the admitted SHA (`admit.reviewers_green != 'true'`) and the PR
  # is non-draft. Same reusable workflows the pipeline uses; the `sha` input posts
  # their statuses to the admitted SHA.
  admit-enforcer:
    name: Admission re-verify — Enforcer
    needs: [authorize, admit]
    if: needs.admit.outputs.reviewers_green != 'true' && github.event.pull_request.draft == false
    uses: ./.github/workflows/agent-enforcer.yml
    with:
      pr_number: ${{ needs.authorize.outputs.pr_number }}
      ref: ${{ needs.authorize.outputs.head_ref }}
      sha: ${{ needs.admit.outputs.admitted_sha }}
    secrets:
      AOPS_BOT_GH_TOKEN: ${{ secrets.AOPS_BOT_GH_TOKEN }}
      CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}

  admit-qa:
    name: Admission re-verify — QA
    needs: [authorize, admit, admit-enforcer]
    # Mirror the pipeline's inverted gradient (§3.4): qa runs whenever the enforcer
    # produced a verdict (success OR failure) and did not commit — a red enforcer
    # must not gate qa off. Skipped only when enforcer was skipped/cancelled, or
    # committed a fix (a new SHA → the pipeline re-verifies that SHA via Stage-2).
    if: >-
      always() &&
      needs.admit.outputs.reviewers_green != 'true' &&
      github.event.pull_request.draft == false &&
      (needs.admit-enforcer.result == 'success' || needs.admit-enforcer.result == 'failure') &&
      needs.admit-enforcer.outputs.committed != 'true'
    uses: ./.github/workflows/agent-qa.yml
    with:
      pr_number: ${{ needs.authorize.outputs.pr_number }}
      ref: ${{ needs.authorize.outputs.head_ref }}
      sha: ${{ needs.admit.outputs.admitted_sha }}
    secrets:
      AOPS_BOT_GH_TOKEN: ${{ secrets.AOPS_BOT_GH_TOKEN }}
      CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}

  # ─── Unresolved third-party comment/review gate (§3.10, PR #2094) — REQUIRED ──
  # Restores the v1 merge-prep invariant ("read ALL reviews — framework agents +
  # Gemini + Copilot + humans", F5) on the admitted SHA. Unlike admit-enforcer/
  # admit-qa/admit-attestation above, this ALWAYS runs on a successful admission
  # (not gated on `reviewers_green`) — comment-triage-status has never been
  # computed for this exact SHA by anything else that necessarily ran before
  # admission, and the check itself is cheap (a few `gh api` calls, no agent
  # invocation), so there is no waste to guard against by skipping it.
  # scripts/ci/check-unresolved-comments.sh (unit-tested,
  # tests/test_check_unresolved_comments.py); fail-closed on fetch error.
  admit-comment-triage:
    name: Admission re-verify — Comment Triage
    needs: [authorize, admit]
    if: needs.admit.result == 'success' && github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    permissions:
      statuses: write
    outputs:
      has_unresolved_comments: ${{ steps.check.outputs.has_unresolved_comments }}
    env:
      GH_TOKEN: ${{ secrets.AOPS_BOT_GH_TOKEN }}
      REPO: ${{ github.repository }}
      PR_NUMBER: ${{ needs.authorize.outputs.pr_number }}
      HEAD_SHA: ${{ needs.admit.outputs.admitted_sha }}
      GITHUB_RUN_ID: ${{ github.run_id }}
    steps:
      - uses: actions/checkout@v4
      - name: Compute comment-triage decision on the admitted SHA
        id: check
        run: bash scripts/ci/check-unresolved-comments.sh
      - name: Post comment-triage-status on the admitted SHA
        if: always()
        run: |
          HAS_UNRESOLVED="${{ steps.check.outputs.has_unresolved_comments }}"
          REASON="${{ steps.check.outputs.reason }}"
          if [ "${{ steps.check.outcome }}" != "success" ]; then
            STATE="failure"
            REASON="check-unresolved-comments.sh step failed to run — failing closed"
          elif [ "$HAS_UNRESOLVED" = "true" ]; then
            STATE="failure"
          else
            STATE="success"
          fi
          DESC="${REASON:0:140}"
          gh api "repos/$REPO/statuses/$HEAD_SHA" \
            -f state="$STATE" \
            -f context="comment-triage-status" \
            -f description="$DESC" \
            -f target_url="https://github.com/$REPO/actions/runs/$GITHUB_RUN_ID?target_sha=$HEAD_SHA"

  admit-attestation:
    name: Admission re-verify — Review Attestation
    needs: [authorize, admit, admit-enforcer, admit-qa]
    # Recompute the REQUIRED review-attestation on the admitted SHA after the
    # reviewers re-ran. The pipeline's own review-attestation last ran on a
    # pre-admission push where the reviewers were skipped, so it is red/absent;
    # this re-posts it green (or honestly red, if the re-verify came back red — the
    # mechanic then clears it). POST-only: unlike the pipeline job it does NOT
    # fail-closed-exit, because a legitimately-red re-verify must not fail this run.
    if: >-
      always() &&
      needs.admit.result == 'success' &&
      needs.admit.outputs.reviewers_green != 'true' &&
      github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    permissions:
      statuses: write
    env:
      GH_TOKEN: ${{ secrets.AOPS_BOT_GH_TOKEN }}
      REPO: ${{ github.repository }}
      HEAD_SHA: ${{ needs.admit.outputs.admitted_sha }}
      GITHUB_RUN_ID: ${{ github.run_id }}
    steps:
      - uses: actions/checkout@v4
      - name: Compute attestation decision on the admitted SHA
        id: attest
        run: ./scripts/ci/review-attestation.sh
      - name: Post review-attestation status on the admitted SHA
        env:
          STATE: ${{ steps.attest.outputs.state }}
          DESC: ${{ steps.attest.outputs.description }}
        run: |
          gh api "repos/$REPO/statuses/$HEAD_SHA" \
            -f state="$STATE" \
            -f context="review-attestation" \
            -f description="$DESC" \
            -f target_url="https://github.com/$REPO/actions/runs/$GITHUB_RUN_ID?target_sha=$HEAD_SHA"

  # ─── Decide the first mechanic dispatch — AFTER any admission re-verification ──
  # The mechanic exists to CLEAR RED (§3.5) AND to RESOLVE CONFLICTS (§3.3/§3.11).
  # Re-read the named reviewer statuses AND the PR's mergeability on the admitted
  # SHA once re-verification has settled, and dispatch the mechanic when EITHER:
  #   (a) a red/pending reviewer signal remains (development to clear it), OR
  #   (b) the PR is CONFLICTING with the base (conflict resolution).
  #
  # (b) is load-bearing and was the prior deadlock (§3.11): a conflicting PR never
  # gets a `pull_request` triage run — GitHub cannot build the `refs/pull/N/merge`
  # ref, so no run fires. Admission via review approval (this workflow, on
  # `pull_request_review`) is the ONLY path that reaches a conflicting PR, and the
  # mechanic dispatched here is the ONLY agent that resolves the conflict. Keying
  # the dispatch on reviewer-colour ALONE meant a conflicting-but-green PR got
  # `need_mechanic=false`, armed an auto-merge that can never fire (CONFLICTING is
  # unmergeable), and nothing ever resolved the conflict — a permanent deadlock.
  # Mergeability is an INDEPENDENT merge gate; reviewers green is not sufficient to
  # skip the mechanic when the PR is CONFLICTING.
  #
  # Only when reviewers are BOTH green, the PR is not conflicting, AND there is no
  # unresolved third-party comment/review (#2094, admit-comment-triage above) does
  # the armed auto-merge handle the merge with no mechanic run. `always()` because
  # admit-enforcer/admit-qa are skipped when the reviewers were already green.
  #
  # Three dispatch conditions, in priority order (a PR can match more than one;
  # the highest-priority match decides the mode — see "Decide mechanic dispatch
  # + mode" below):
  #   (a) a red/pending reviewer signal remains → Stage-2 mode (clear the red).
  #   (b) the PR is CONFLICTING with the base → Stage-2 mode (resolve the conflict,
  #       §3.11 — this is why CONFLICTING is checked regardless of reviewer colour).
  #   (c) reviewers green, not conflicting, but an unresolved third-party comment
  #       or review body remains → review-response mode (comment-scoped triage
  #       only; #2094). This is NEW: a PR matching only (c) used to get
  #       `need_mechanic=false` and merge immediately once armed — the incident
  #       this whole gate exists to close. If a PR ALSO matches (a) or (b), those
  #       take priority for THIS pass; comment-triage is deferred to the next
  #       pass once the mechanic's own commit re-triggers this decision — safe
  #       either way, because `comment-triage-status` (a REQUIRED check,
  #       independent of this dispatch decision) blocks merge on its own until
  #       the comments are actually addressed, regardless of dispatch ordering.
  decide-mechanic:
    name: Decide mechanic dispatch
    needs: [authorize, admit, admit-enforcer, admit-qa, admit-comment-triage]
    if: always() && needs.admit.result == 'success' && github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    outputs:
      need_mechanic: ${{ steps.decide.outputs.need_mechanic }}
      mechanic_mode: ${{ steps.decide.outputs.mechanic_mode }}
    env:
      GH_TOKEN: ${{ secrets.AOPS_BOT_GH_TOKEN || github.token }}
      REPO: ${{ github.repository }}
      HEAD_SHA: ${{ needs.admit.outputs.admitted_sha }}
      PR_NUMBER: ${{ needs.authorize.outputs.pr_number }}
    steps:
      - name: Fetch reviewer state + mergeability on the admitted SHA
        id: fetch
        run: |
          ENF=$(gh api "repos/$REPO/commits/$HEAD_SHA/statuses" \
            --jq '[.[] | select(.context == "enforcer-status")] | sort_by(.created_at) | last.state // ""' 2>/dev/null || echo "")
          QA=$(gh api "repos/$REPO/commits/$HEAD_SHA/statuses" \
            --jq '[.[] | select(.context == "qa-status")] | sort_by(.created_at) | last.state // ""' 2>/dev/null || echo "")

          # Mergeability is computed asynchronously by GitHub; poll until it
          # settles (UNKNOWN → MERGEABLE/CONFLICTING). By admission time it is
          # almost always already computed; the poll is a short safety margin.
          MERGEABLE="UNKNOWN"
          for _ in 1 2 3 4 5; do
            MERGEABLE=$(gh pr view "$PR_NUMBER" --repo "$REPO" --json mergeable --jq '.mergeable' 2>/dev/null || echo "UNKNOWN")
            [ "$MERGEABLE" != "UNKNOWN" ] && break
            sleep 3
          done

          echo "enforcer_status=$ENF" >> "$GITHUB_OUTPUT"
          echo "qa_status=$QA" >> "$GITHUB_OUTPUT"
          echo "mergeable=$MERGEABLE" >> "$GITHUB_OUTPUT"

      # Pure decision over already-fetched signals — no live `gh` calls here, so
      # this step is extracted and unit-tested directly
      # (tests/test_decide_mechanic_wiring.py), mirroring the convention in
      # tests/test_agent_enforcer_terminal_status_wiring.py.
      - name: Decide mechanic dispatch + mode
        id: decide
        env:
          ENF: ${{ steps.fetch.outputs.enforcer_status }}
          QA: ${{ steps.fetch.outputs.qa_status }}
          MERGEABLE: ${{ steps.fetch.outputs.mergeable }}
          HAS_UNRESOLVED_COMMENTS: ${{ needs.admit-comment-triage.outputs.has_unresolved_comments }}
        run: |
          echo "Final enforcer-status='${ENF:-absent}' qa-status='${QA:-absent}' mergeable='${MERGEABLE}' unresolved_comments='${HAS_UNRESOLVED_COMMENTS:-false}' on $HEAD_SHA"
          if [ "$MERGEABLE" = "CONFLICTING" ]; then
            echo "PR is CONFLICTING — dispatching mechanic (Stage-2) to resolve the conflict (§3.11), regardless of reviewer colour or comments."
            echo "need_mechanic=true" >> "$GITHUB_OUTPUT"
            echo "mechanic_mode=" >> "$GITHUB_OUTPUT"
          elif [ "$ENF" != "success" ] || [ "$QA" != "success" ]; then
            echo "Red/pending reviewer signal — dispatching mechanic (Stage-2) to clear it."
            echo "need_mechanic=true" >> "$GITHUB_OUTPUT"
            echo "mechanic_mode=" >> "$GITHUB_OUTPUT"
          elif [ "$HAS_UNRESOLVED_COMMENTS" = "true" ]; then
            echo "Reviewers green and PR mergeable, but an unresolved third-party comment/review remains (#2094) — dispatching mechanic in review-response mode to triage before merge."
            echo "need_mechanic=true" >> "$GITHUB_OUTPUT"
            echo "mechanic_mode=review-response" >> "$GITHUB_OUTPUT"
          else
            echo "Both reviewers green, PR not conflicting, no unresolved comments — auto-merge handles it; mechanic not needed."
            echo "need_mechanic=false" >> "$GITHUB_OUTPUT"
            echo "mechanic_mode=" >> "$GITHUB_OUTPUT"
          fi

  # ─── Review-response path: changes_requested ────────────────────────────────
  # When a write-class maintainer submits a CHANGES_REQUESTED review on a ready
  # (non-draft) PR, dispatch the mechanic in comment-scoped mode. This addresses
  # reviewer feedback only — it does NOT admit, does NOT set admit-status, does
  # NOT arm auto-merge. The human's CHANGES_REQUESTED stands; merge happens only
  # after a subsequent human Approve (the approve path above).
  #
  # Boundary vs pre-admission-responder (§3.8):
  #   pre-admission-responder = mechanical CI/conflict red, triggered by push
  #   review-response         = human reviewer comments, triggered by this review
  # The de-conflict guard below checks responder-status on HEAD; if the responder
  # is currently pending (in-progress), we skip to avoid concurrent development
  # on the same SHA. They use distinct concurrency groups and distinct status
  # contexts (review-response-status vs responder-status), so they do not
  # cancel each other — the guard is a best-effort double-develop shield.
  authorize-changes:
    name: Authorize review-response dispatch
    if: >-
      github.event.review.state == 'changes_requested' &&
      github.event.pull_request.head.repo.full_name == github.repository &&
      github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    outputs:
      decision: ${{ steps.decide.outputs.decision }}
      head_ref: ${{ github.event.pull_request.head.ref }}
      pr_number: ${{ github.event.pull_request.number }}
      head_sha: ${{ steps.resolve.outputs.head_sha }}
    env:
      GH_TOKEN: ${{ secrets.AOPS_BOT_GH_TOKEN || github.token }}
      REPO: ${{ github.repository }}
      REVIEWER_LOGIN: ${{ github.event.review.user.login }}
      # ADMIT_ALLOWLIST is intentionally NOT set here — the decide step below
      # sources scripts/ci/reviewer-authz.sh, which supplies the single
      # default (pr-pipeline.md §5.1) shared by every admission path.
      PR_NUMBER: ${{ github.event.pull_request.number }}
    steps:
      - uses: actions/checkout@v4
      - name: Resolve reviewer repository permission
        id: perm
        run: |
          PERM=$(gh api "repos/$REPO/collaborators/$REVIEWER_LOGIN/permission" \
            --jq '.permission' 2>/dev/null || echo "none")
          echo "Reviewer $REVIEWER_LOGIN permission: $PERM"
          echo "permission=$PERM" >> "$GITHUB_OUTPUT"
      - name: Resolve live HEAD SHA
        id: resolve
        run: |
          HEAD_SHA=$(gh pr view "$PR_NUMBER" --repo "$REPO" --json headRefOid --jq '.headRefOid')
          echo "Live HEAD SHA for PR #$PR_NUMBER: $HEAD_SHA"
          echo "head_sha=$HEAD_SHA" >> "$GITHUB_OUTPUT"
      - name: Decide — write-class check + responder de-conflict guard
        id: decide
        env:
          REVIEWER_PERMISSION: ${{ steps.perm.outputs.permission }}
          HEAD_SHA: ${{ steps.resolve.outputs.head_sha }}
        run: |
          set -euo pipefail

          # Write-class authorization — shares scripts/ci/reviewer-authz.sh
          # with admit-on-review.sh and find-conflicting-admitted-prs.sh
          # (pr-pipeline.md §5.1) so this policy can't drift between
          # admission paths. Sourcing also supplies the ADMIT_ALLOWLIST
          # default when this job hasn't set one itself.
          source scripts/ci/reviewer-authz.sh
          if ! is_authorized_reviewer "$REVIEWER_LOGIN" "$REVIEWER_PERMISSION" "$ADMIT_ALLOWLIST"; then
            echo "Skipped: $REVIEWER_LOGIN (permission: $REVIEWER_PERMISSION) is not write-class — no dispatch"
            echo "decision=skip" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          # De-conflict guard: if the pre-admission responder is currently in-progress
          # on this SHA, defer to avoid concurrent development on the same branch.
          RESPONDER_STATE=$(gh api "repos/$REPO/commits/$HEAD_SHA/statuses?per_page=100" \
            --jq '[.[] | select(.context == "responder-status")] | sort_by(.created_at) | last.state // ""' \
            2>/dev/null || echo "")
          if [[ "$RESPONDER_STATE" == "pending" ]]; then
            echo "Pre-admission responder is in-progress on $HEAD_SHA — deferring review-response dispatch to avoid double-develop"
            echo "decision=skip" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          echo "decision=authorized" >> "$GITHUB_OUTPUT"
          echo "Authorized: $REVIEWER_LOGIN (permission: $REVIEWER_PERMISSION) on $HEAD_SHA — dispatching review-response mechanic"

  mechanic-review-response:
    name: Mechanic (review-response — comment-scoped)
    needs: [authorize-changes]
    if: needs.authorize-changes.outputs.decision == 'authorized'
    uses: ./.github/workflows/agent-mechanic.yml
    with:
      pr_number: ${{ needs.authorize-changes.outputs.pr_number }}
      ref: ${{ needs.authorize-changes.outputs.head_ref }}
      sha: ${{ needs.authorize-changes.outputs.head_sha }}
      mode: review-response
    secrets:
      AOPS_BOT_GH_TOKEN: ${{ secrets.AOPS_BOT_GH_TOKEN }}
      CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}

  # ─── First mechanic dispatch (the hop the Environment gate could not make) ──
  # Subsequent passes (2…N) are dispatched by pr-pipeline.yml's check-admit →
  # mechanic on the synchronize events the mechanic's commits fire; admit-status
  # carries forward across those agent commits (§5), so there is no race there.
  #
  # Gated on `decide-mechanic` (not `admit`) so the dispatch decision is made AFTER
  # any admission-boundary re-verification has settled — the §3.1 fire-once gate
  # means the admitted SHA's reviewer verdicts may only exist once admit-enforcer/
  # admit-qa have re-run here. decide-mechanic depends on those jobs, so this waits.
  mechanic:
    name: Mechanic (Stage 2 — first dispatch)
    needs: [authorize, admit, decide-mechanic]
    # always() because decide-mechanic itself only ran via its own always()
    # override (it depends on admit-enforcer/admit-qa, which are skipped or can
    # fail when reviewers were already green or genuinely red). Without this,
    # GitHub Actions' implicit success() prefix on a bare `if:` inherits that
    # transitive failure through decide-mechanic and silently skips this job
    # even when decide-mechanic correctly computed need_mechanic=true — the
    # exact bug that stranded PR #2073 (issue #2084). Mirrors the same
    # always()-plus-explicit-admit-check pattern decide-mechanic and
    # admit-attestation already use.
    if: >-
      always() &&
      needs.admit.result == 'success' &&
      needs.decide-mechanic.outputs.need_mechanic == 'true' &&
      github.event.pull_request.draft == false
    uses: ./.github/workflows/agent-mechanic.yml
    with:
      pr_number: ${{ needs.authorize.outputs.pr_number }}
      ref: ${{ needs.authorize.outputs.head_ref }}
      sha: ${{ needs.admit.outputs.admitted_sha }}
      # '' (Stage-2 default) when dispatched for red/conflict; 'review-response'
      # when the ONLY reason is an unresolved comment/review (#2094) —
      # mechanic.agent.md's Stage-2 mandate carries no comment-triage
      # instructions, so passing the wrong mode here would silently no-op on
      # exactly the case this gate exists to fix.
      mode: ${{ needs.decide-mechanic.outputs.mechanic_mode }}
    secrets:
      AOPS_BOT_GH_TOKEN: ${{ secrets.AOPS_BOT_GH_TOKEN }}
      CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
