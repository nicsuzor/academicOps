# CI Pipeline: lint → typecheck → pytest (sequential)
#
# Sequential ordering avoids wasted compute when lint pushes autofix
# commits — typecheck and pytest haven't started yet, so the cancelled
# run discards no work. The new synchronize event restarts the pipeline
# on the clean commit.
#
# Axiom Review runs as a separate workflow (axiom-review.yml).
# Agent Fix has been removed — merge-prep handles all fix work.
#
# See specs/pr-pipeline.md for the full process documentation.

name: PR Review Pipeline

on:
  pull_request:
    types: [opened, synchronize, ready_for_review, reopened]

permissions:
  contents: write
  pull-requests: write
  statuses: write
  id-token: write
  issues: write
  actions: read

concurrency:
  group: pr-pipeline-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  # ─── Step 1: Lint ─────────────────────────────────────────────────
  # If lint autofixes and pushes (using AOPS_BOT_GH_TOKEN), the push
  # triggers a new synchronize event. This run is cancelled and the
  # pipeline restarts on the clean commit.
  lint:
    name: Lint
    uses: ./.github/workflows/lint.yml
    secrets: inherit

  # ─── Step 2: Type Check ──────────────────────────────────────────
  typecheck:
    name: Type Check
    needs: [lint]
    uses: ./.github/workflows/typecheck.yml
    secrets: inherit

  # ─── Step 3: Pytest ──────────────────────────────────────────────
  pytest:
    name: Pytest
    needs: [typecheck]
    uses: ./.github/workflows/pytest.yml
    secrets: inherit
