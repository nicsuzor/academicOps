{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "$comment": "Web-compatible settings for Claude Code Web environments. No hooks (require $AOPS). Skills, commands, agents work via bundled .claude/ directory.",
  "permissions": {
    "allow": [
      "Read"
    ],
    "deny": []
  }
}
