---
name: example
description: Example command.
---

# /example

This is an example command. Commands act like slash commands (e.g. `/example`) that users can trigger.

When the user types `/example`, the agent will read these instructions and execute them.
