# No [[shared]] entries. ida's only dependency on lib/ is the doctrine it
# `@include`s, and the build resolves those against lib/ directly — injecting
# doctrine/ into the plugin tree would ship a second copy of it.
