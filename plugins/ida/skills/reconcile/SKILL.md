---
name: reconcile
description: Retired. Replaced by the universal workflow template wf-reconcile.
---

# Reconcile Task Graph (Retired)

The `/reconcile` skill has been retired and replaced by a workflow template.
To run the reconcile procedure, invoke the workflow library:
`/pull wf-reconcile`

The standard obligations, exit criteria, and status rulings for this pass are now maintained centrally at `workflows/wf-reconcile.md`.
