---
name: reconcile
description: Truth maintenance over the task graph -- redirects to universal workflow template wf-reconcile.
---

# Reconcile

The canonical reconcile procedure is defined in universal workflow template `wf-reconcile`.

Run the pass by resolving and executing `wf-reconcile` via `/pull wf-reconcile` or through the workflow library (`plugins/ida/skills/workflow-library/workflows/wf-reconcile.md`).
