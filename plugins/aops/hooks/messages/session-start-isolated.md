A GitHub token and git credential configuration are present in this session's environment, and in the session environment file this client sources. `git` and `gh` authenticate on their own — you never need to read or supply a credential. Read none, print none, and copy none into a file, a commit message, or another agent's prompt. If a task appears to require handling a credential yourself, say so and stop.
