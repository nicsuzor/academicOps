<aops-warning>You have uncommitted changes. You MUST commit and push your work before ending the session.

**Required steps:**

1. Stage your changes with `git add`
2. Create a commit with a descriptive message
3. Push to the remote branch

Do NOT end the session until all changes are committed and pushed.
</aops-warning>
